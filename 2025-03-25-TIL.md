## 프로젝트 주제

이 프로젝트는 고객이 자신의 자산을 효율적으로 관리할 수 있도록 돕는 시스템을 개발하는 것을 목표로 합니다. 특히, 챗 봇 기능을 통해 사용자가 자신의 자산 정보를 쉽게 조회하고 관리할 수 있도록 지원합니다. 주식, 현금, 부동산, 가상 자산 등 다양한 자산을 관리할 수 있는 기능을 제공하며, 사용자와의 자연스러운 대화를 통해 자산 관련 정보를 제공합니다.

## 프로젝트 개요

- 고객 자산 관리 시스템의 사용자 경험을 향상시키기 위해 챗 봇 기능을 통합합니다.
- 챗 봇을 통해 자산 관리에 필요한 정보를 제공받을 수 있도록 합니다.

## 요구 사항 정의서

1. **자산 관리**
    
    유저 별로 현금, 주식, 부동산, 가상 자산 등 소유한 자산을 입력할 수 있습니다. (우선적으로 주식에 대해서 자세히 구현한 이후, 여유 기간에 따라 추가 구현)
    
2. **챗 봇 기능**
    
    유저들은 자신이 소유한 자산이나 현재 자산에 대해 챗 봇 기능을 통해 자유롭게 소통할 수 있습니다.
    
3. **DB**
    
    유저가 입력한 자산 정보를 DB에 기록할 수 있습니다.
    
    유저가 챗 봇을 종료할 때, 제목, 요약,  채팅 내용을 DB에 기록할 수 있습니다. 
    
4. **파일 입출력**
    
    유저의 채팅 내역을 JSON 형식을 통해 내보낼 수 있습니다.
    
    JSON 형식의 파일을 최종 보고서(javascript, html) 형태로 확인할 수 있습니다.
    

## ERD

```sql
CREATE TABLE "USER"
(
    "USER_ID"    VARCHAR2(255) PRIMARY KEY,
    "NAME"       VARCHAR2(255),
    "PASSWORD"   VARCHAR(255),
    "CREATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "UPDATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "DELETED_AT" TIMESTAMP DEFAULT NULL
);

CREATE TABLE "ASSET"
(
    "ASSET_ID"   NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "USER_ID"    VARCHAR2(255),
    "CATEGORY"   NUMBER,
    "AMOUNT"     NUMBER,
    "CREATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "UPDATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "DELETED_AT" TIMESTAMP DEFAULT NULL
);

CREATE TABLE "STOCK"
(
    "STOCK_ID"     NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "STOCK_TICKER" VARCHAR2(255),
    "NAME"         VARCHAR2(255),
    "SECTOR"       VARCHAR2(255),
    "CREATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "UPDATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "DELETED_AT" TIMESTAMP DEFAULT NULL
);

CREATE TABLE "USER_STOCK"
(
    "USER_STOCK_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "USER_ID"       VARCHAR2(255),
    "STOCK_ID"      NUMBER,
    "QUANTITY"      NUMBER,
    "TOTAL_PRICE"   NUMBER,
    "CREATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "UPDATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "DELETED_AT" TIMESTAMP DEFAULT NULL
);

CREATE TABLE "CHAT"
(
    "CHAT_ID"    NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "USER_ID"    VARCHAR2(255),
    "TITLE"      VARCHAR2(255),
    "CONTENT"    CLOB,
    "SUMMARY"    CLOB,
    "CREATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "UPDATED_AT" TIMESTAMP DEFAULT SYSTIMESTAMP,
    "DELETED_AT" TIMESTAMP DEFAULT NULL
);
```
