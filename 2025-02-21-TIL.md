# 📋 MyBatis의 `selectKey` 사용 및 외래키 삭제 이슈 정리

---

## 🟢 **1. MyBatis의 `selectKey` 사용법 및 객체와 DB 값 불일치 해결 방법**

### 📌 **문제 요약**
- **상황**: 서비스 계층에서 `Comment` 객체를 생성하여 DAO를 통해 DB에 INSERT할 때, 작성일을 `NOW()`로 설정하면, 객체에는 DB의 작성일 값이 반영되지 않음.
- **결과**: 클라이언트에 반환되는 객체가 실제 DB에 저장된 값과 불일치.

### 💡 **해결 방법**

#### ✅ **1.1. MyBatis의 `selectKey` 사용**
- **기능**: INSERT 쿼리 실행 후, DB에서 생성된 값을 객체에 반영.
- **장점**: 추가 조회 없이 한 번의 쿼리 실행으로 DB 값 반영 가능.

**예시 코드:**
```xml
<insert id="insertBoard" parameterType="Board" useGeneratedKeys="true" keyProperty="id">
    <![CDATA[
    INSERT INTO BOARD (title, content, userId)
    VALUES (#{title}, #{content}, #{userId})
    ]]>
    <selectKey keyProperty="id" resultType="int" order="AFTER">
        SELECT board_seq.CURRVAL FROM dual
    </selectKey>
</insert>
```

- **주의사항**: `board_seq` 같은 시퀀스 객체가 필요함.

#### ✅ **1.2. 추가 조회(재조회) 방법**
- **방법**: INSERT 후 해당 레코드를 다시 조회하여 작성일 등 DB에 저장된 값을 반영.
- **장점**: DB 상태를 정확히 반영 가능.
- **단점**: 추가 조회로 인한 성능 오버헤드 발생 가능.

#### ✅ **1.3. `selectKey` 사용 시 주의점**
- **BEFORE vs AFTER**:
  - **BEFORE**: INSERT 전에 키 값을 생성 (ex: 커스텀 ID 생성).
  - **AFTER**: INSERT 후 DB에서 생성된 값을 조회 (ex: 시퀀스 사용).

---

## 🟡 **2. 외래키 `ON DELETE CASCADE` 사용 시 삭제 이슈**

### 📌 **문제 요약**
- **상황**: 외래키에 `ON DELETE CASCADE`를 설정했지만, 삭제 시 오류 발생 또는 무한 대기 현상.
- **원인**:
  1. **잠금(Lock) 및 트랜잭션 문제**: 자식 테이블에 락이 걸린 경우, 삭제 작업이 대기.
  2. **인덱스 및 트리거 충돌**: 외래키 인덱스 또는 트리거가 삭제 작업에 영향을 줄 수 있음.

### 💡 **해결 방법**

#### ✅ **2.1. 잠금 및 트랜잭션 확인**
- **방법**:
  - 데이터베이스에서 락 상태 확인 (`SHOW ENGINE INNODB STATUS;`)
  - 미완료 트랜잭션 종료 후 다시 삭제 시도.

#### ✅ **2.2. 외래키 인덱스 최적화**
- **방법**:
  - 외래키 컬럼에 적절한 인덱스 추가.
  - 불필요한 트리거 제거.

---

## ⚖️ **3. 실무에서 외래키 설정 사용 여부**

### ✅ **3.1. 외래키를 사용하는 경우**
- **데이터 무결성 보장**: 부모-자식 간 참조 무결성 유지.
- **자동 연쇄 삭제**: `ON DELETE CASCADE`를 통해 자식 데이터 자동 삭제.

### ⚠️ **3.2. 외래키를 사용하지 않는 경우**
- **어플리케이션 단에서 무결성 관리**: 대규모 데이터베이스에서 성능 최적화를 위해 사용.
- **대용량 데이터 처리 시**: 외래키의 검사 비용으로 인한 성능 저하 방지.

---

## 🏆 **결론**
- **MyBatis**: `selectKey`를 통해 INSERT 후 생성된 값을 객체에 반영하거나, 추가 조회를 통해 정확한 데이터 반환.
- **외래키 관리**: 일반적으로 데이터 무결성을 위해 사용하지만, 성능 최적화가 필요한 경우 어플리케이션에서 관리.
- **삭제 이슈**: 잠금 및 트랜잭션 관리에 유의하며, 외래키 인덱스와 트리거를 점검.
