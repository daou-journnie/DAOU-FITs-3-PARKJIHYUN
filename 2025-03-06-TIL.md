## 문자열 및 기본 입출력 함수

### 문자열 (String)

- **정의:**
    - 문자열은 `char`의 배열이며, 마지막에 널 문자(`'\0'`)로 종료
- **입출력 함수:**
    - **입력:** `gets()` (주의: 보안 문제로 현대에는 사용 권장하지 않음), `fgets()`
    - **출력:** `puts()`

### 단일 문자 입출력

- **정의:**
    - 단일 문자는 `char` 자료형으로 표현.
- **입출력 함수:**
    - **입력:** `getchar()`
    - **출력:** `putchar()`

---

# 연산자 (Operators)

프로그램에서 변수나 상수에 대해 연산(계산, 비교, 대입 등)을 수행할 때 사용되는 기호 혹은 기호들의 집합

### 산술연산자 (Arithmetic Operators)

- **기본 기호:**
    - `+`, , , `/`, `%`
    - `%`는 정수형에만 적용되어 나머지 연산 수행
    - 단항 는 부호 변경 역할
- **자료형 혼합 시 결과:**
    - `int + int` → `int`
    - `int + float` → `float`
    - `int + char` → `int`
    - `char + char` → `int` (보통 정수 연산으로 처리)
    - `char + float` → `float`
    - `float + float` → `float`

### 대입연산자 (Assignment Operators)

- **기본 대입:**
    - `=`
- **복합 대입:**
    - `+=`, `=`, `=`, `/=`, `%=` 등
- **예제:**
    - `a = 1; a += 2; a -= 4;`

### 비교연산자 (Relational/Comparison Operators)

- **기본 기호:**
    - `==`, `<`, `>`, `<=`, `>=`, `!=`
- **기능:**
    - 두 피연산자를 비교하여 참(true) 또는 거짓(false) 반환

### 논리연산자 (Logical Operators)

- **기본 기호:**
    - `&&`, `||`, `!`
- **용도:**
    - 조건문에서 여러 조건을 결합하여 참/거짓 판단

### 증감연산자 (Increment/Decrement Operators)

- **기호:**
    - `++` (증가), `-` (감소)
- **전위와 후위:**
    - `++x` (전위): 먼저 증가 후 사용
    - `x++` (후위): 현재 값 사용 후 증가

### 비트 연산자 (Bitwise Operators)

- **비트 논리연산자:**
    - `&` (AND), `|` (OR), `^` (XOR), `~` (NOT)
- **시프트 연산자:**
    - 왼쪽 시프트 `<<`: 값이 2의 n제곱만큼 곱해짐
    - 오른쪽 시프트 `>>`: 값이 2의 n제곱만큼 나눠지며 소수점은 버림

### 콤마연산자 (Comma Operator)

- **용도:**
    - 여러 표현식을 왼쪽부터 오른쪽 순으로 실행하고 마지막 표현식 결과 반환
- **예제:**
    - `a = (b = 1, b + 2);`

### 삼항연산자 (Ternary Operator)

- **형식:**
    - `조건 ? 항목1 : 항목2`
- **용도:**
    - 조건에 따라 간단하게 if-else를 한 줄로 표현

### 형변환연산자 (Type Casting Operator)

- **형식:**
    - `(자료형) 변수명`
- **용도:**
    - 변수의 실제 타입은 그대로 두고 연산 중에 임시로 다른 타입으로 변환

### sizeof() 연산자

- **용도:**
    - 자료형 또는 변수의 메모리 크기를 반환
- **예제:**
    - `sizeof(int)`, `sizeof(변수명)`

### 부호 지정 (Signed vs. Unsigned)

- **부호 있는 정수/문자:**
    - 음수와 양수 모두 표현 (예: `int`, `char` – 단, `char`는 구현에 따라 부호 여부가 다를 수 있음)
- **부호 없는 정수/문자:**
    - 0 이상의 값만 표현, 더 넓은 양의 정수 범위 제공 (예: `unsigned int`, `unsigned char`)

### 1의 보수와 2의 보수

- **1의 보수 (One's Complement):**
    - **정의:** 모든 비트를 반전(0↔1)하여 얻는 값
    - **특징:**
        - +0과 -0 두 가지가 존재
        - 덧셈 시 보정(carry) 필요
- **2의 보수 (Two's Complement):**
    - **정의:** 1의 보수에 1을 더한 값
    - **특징:**
        - 유일한 0 (0000…0)만 존재
        - 산술 연산이 간단해 하드웨어 구현에 적합
        - n비트에서 표현 범위: -2^(n-1) ~ 2^(n-1)-1

---

# 제어문 (Control Statements)

프로그램의 흐름을 조건이나 반복에 따라 제어하는 구문

## 조건문 (Selection Statements)

### if문

- **단순 if문 예제: "오늘의 온도 구하기"**
    - 입력한 온도가 0 이하이면 "영하"와 온도 출력
    - 예시
        - 입력: -2 → "오늘은 영하이며 현재 온도는 -2도입니다"
        - 입력: 5 → "현재 온도는 5도입니다"
- **if~else문 예제: 정수 홀짝 및 양/음 판별**
    - 입력: -3 → "입력한 수 -3은 음의 홀수입니다"
    - 입력: 5 → "입력한 수 5는 양의 홀수입니다"
- **if~~else if~~else문 예제: 점수에 따른 학점 계산**

### switch~case문

- **용도:**
    - 변수의 값에 따라 여러 경우로 분기
- **예제:**
    - 산술 계산기, 메뉴 선택 등
    - 입력한 정수(메뉴 번호)에 따라 각 case 블록 실행

## 반복문 (Loop Statements)

### for문

- **구조:**
    
    ```c
    for (초기값; 조건식; 증감식) {
        // 반복할 내용
    }
    
    ```
    
- **예제 문제:**
    - 1~100까지의 합, 3의 배수 출력, 역순 출력, 구구단, 패턴 출력, 팩토리얼, 피보나치 수열 등

### while문

- **구조:**
    
    ```c
    while (조건식) {
        // 반복할 내용
    }
    
    ```
    
- **예제 문제:**
    - 1~100까지 짝수의 합, 특정 입력(예: 999)이 될 때까지 입력 받기, 조건에 따른 누적 합 계산 (continue 사용)

### do~while문

- **구조:**
    
    ```c
    do {
        // 반복할 내용
    } while (조건식);
    
    ```
    
- **특징:**
    - 반복문 내부 내용은 최소 1회 실행됨
- **예제 문제:**
    - 메뉴 선택 프로그램 (예: 원의 둘레, 넓이, 구의 부피 계산 등)
