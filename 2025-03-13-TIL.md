## 1. 정적 2차원 배열: `int array[3][3]`

```c
int array[3][3] = {
    {0, 1, 2},
    {3, 4, 5},
    {6, 7, 8}
};

```

- **메모리 배치:**
이 배열은 9개의 `int`가 연속된 메모리 블록에 저장
각 행은 연속된 3개의 `int`로 구성
- **타입:**
    - `array` 자체의 타입은 `int[3][3]`
    - 표현식에서 `array`는 **decay**되어 `int (*)[3]` (즉, “3개의 int를 담고 있는 배열에 대한 포인터”)로 취급
    - `array + 1`  == 두 번째 행의 주소 (`&array[1]`)

---

## 2. 포인터 배열: `int* arr[3]`

```c
int* arr[3] = { array[0], array[1], array[2] };

```

- **메모리 배치:**
이 선언은 크기가 3인 포인터 배열을 만듦
각 요소는 `int*` 타입으로,`array[0]`, `array[1]`, `array[2]`는 각각 첫 번째, 두 번째, 세 번째 행의 시작 주소 (`&array[0][0]`, `&array[1][0]`, `&array[2][0]`)
- **타입:**
    - `arr`의 타입은 **배열**이며, 각 요소의 타입은 `int*`
    - `arr` == “포인터들의 배열”

---

## 3. 비교: `array` vs. `arr`

### a. 메모리 구조 및 타입

- **`array` (또는 `array`의 decay된 형태 `int (*)[3]`):**
    - 메모리는 **연속된 9개의 정수**로 구성
    - 포인터 산술을 할 때, 한 칸 이동하면 한 행(3개의 int) 단위로 건너뜀
- **`arr`:**
    - `arr`은 3개의 포인터를 저장하는 **배열**
    - 각 포인터는 별도의 메모리 주소를 가리키며, 이 주소들은 **연속되지 않을 수도** 
    (이 경우에는 모두 동일한 2차원 배열의 각 행을 가리키도록 초기화했으므로, 각 포인터는 연속된 3개의 int 배열의 시작 주소를 가리킴)
    - 포인터 산술을 할 때, `arr + 1`은 두 번째 포인터를 가리킴

### b. 사용 용도

- **`array`를 단일 포인터처럼 사용:**
    - 함수의 인자로 `array`를 넘기면, 이는 decay되어 `int (*)[3]` 타입

```c
void func(int arr[][3]) { /* ... */ }
func(array);
// 이렇게 하면 함수 내부에서 arr[i][j]로 접근할 수 있음
```

- **`arr` (포인터 배열)을 사용하는 경우:**
    - `arr`의 각 요소는 `int*`이므로, `arr[i]`는 i번째 행의 시작 주소 (`&array[i][0]`)
    - 이 상태에서는 `arr[i][j]`도 정상적으로 작동
    다만, **포인터 배열은 원래 각 행이 반드시 연속적일 필요가 없으므로, 동적 할당 등에서 유용**
