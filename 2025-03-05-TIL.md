## 1. 정수형 (Integer Types)

정수형은 부호 있는(signed) 형태와 부호 없는(unsigned) 형태로 구분됩니다.

### 1.1. 부호 있는 정수 (Signed Integers)

### 1.1.1. 10진수 정수

- **리터럴 표현법:**
그냥 숫자를 그대로 사용
    - 예: `2025`, `123`
- **출력 포맷:**`%d` 또는 `%i`
    - 예:
        
        ```c
        int num = 2025;
        printf("%d", num);
        
        ```
        
- **입력 포맷:**`%d`
    - 예:
        
        ```c
        int num;
        scanf("%d", &num);
        
        ```
        

### 1.1.2. 8진수 정수

- **리터럴 표현법:**
숫자 앞에 `0`을 붙임
    - 예: `0261` (10진수 177)
- **출력 포맷:**`%o`
    - 예:
        
        ```c
        int oct = 0261;
        printf("%o", oct);
        
        ```
        
- **입력 포맷:**`%i`나 `%o`를 사용할 수 있으나, 일반적으로 10진수 입력과 혼동이 없도록 `%i`를 사용하는 경우가 많습니다.
    - 예:
        
        ```c
        int oct;
        scanf("%i", &oct);  // "0261"으로 입력하면 8진수로 해석됨
        
        ```
        

### 1.1.3. 16진수 정수

- **리터럴 표현법:**
숫자 앞에 `0x` 또는 `0X`를 붙임
    - 예: `0x7dd`, `0xA2C`
- **출력 포맷:**`%x` (소문자) 또는 `%X` (대문자)
    - 예:
        
        ```c
        int hex = 0xA2C;
        printf("%x", hex);
        
        ```
        
- **입력 포맷:**`%i`도 사용 가능하며, 입력 시 0x를 포함하면 16진수로 인식됨
    - 예:
        
        ```c
        int hex;
        scanf("%i", &hex);  // "0xA2C"로 입력하면 16진수로 처리
        
        ```
        

### 1.2. 부호 없는 정수 (Unsigned Integers)

### 1.2.1. 10진수 정수

- **리터럴 표현법:**
숫자 뒤에 `u` 또는 `U`를 붙임
    - 예: `2025u`
- **출력 포맷:**`%u`
    - 예:
        
        ```c
        unsigned int unum = 2025U;
        printf("%u", unum);
        
        ```
        
- **입력 포맷:**`%u`
    - 예:
        
        ```c
        unsigned int unum;
        scanf("%u", &unum);
        
        ```
        

### 1.2.2. 8진수 정수

- **리터럴 표현법:**
앞에 `0`을 붙이고 뒤에 `u` 또는 `U`를 붙임
    - 예: `0261u`
- **출력 포맷:**`%o`
    - 예:
        
        ```c
        unsigned int uoct = 0261U;
        printf("%o", uoct);
        
        ```
        
- **입력 포맷:**`%i`를 사용하면 0으로 시작하는 입력은 8진수로 해석됨
    - 예:
        
        ```c
        unsigned int uoct;
        scanf("%i", &uoct);
        
        ```
        

### 1.2.3. 16진수 정수

- **리터럴 표현법:**
앞에 `0x` 또는 `0X`를 붙이고 뒤에 `u` 또는 `U`를 붙임
    - 예: `0x7ddU`, `0xA2CU`
- **출력 포맷:**`%x` 또는 `%X`
    - 예:
        
        ```c
        unsigned int uhex = 0xA2CU;
        printf("%x", uhex);
        
        ```
        
- **입력 포맷:**`%i`를 사용하면 0x 접두어가 있는 경우 16진수로 인식됨
    - 예:
        
        ```c
        unsigned int uhex;
        scanf("%i", &uhex);
        
        ```
        

---

## 2. 실수형 (Floating-Point Types)

실수형은 부호가 포함되어 있으며, float, double, long double 등이 있습니다. 실수형에는 별도의 부호 지정자는 사용하지 않습니다.

### 2.1. 소수점 표기법

- **리터럴 표현법:**
소수점을 포함한 숫자 그대로 사용
    - 예: `3.14`, `0.005`
    - **Float 상수:** 끝에 `f` 또는 `F`를 붙임
        - 예: `3.14f`
- **출력 포맷:**`%f`
    - 예:
        
        ```c
        double pi = 3.14;
        printf("%f", pi);
        
        ```
        
    - 소수점 자릿수 지정: `%.nf` (예: `%.2f`)
- **입력 포맷:**
    - **double:** `%lf`
        - 예:
            
            ```c
            double num;
            scanf("%lf", &num);
            
            ```
            
    - **float:** `%f`
        - 예:
            
            ```c
            float num;
            scanf("%f", &num);
            
            ```
            

### 2.2. 지수(Exponential) 표기법

- **리터럴 표현법:**`e` 또는 `E`를 사용하여 표현
    - 예: `0.314e1`, `3.14e+2`
- **출력 포맷:**`%e` 또는 `%E`
    - 예:
        
        ```c
        double num = 3.14e+2;
        printf("%e", num);
        
        ```
        
- **입력 포맷:**
    - double은 `%lf`, float은 `%f`를 사용합니다 (지수형 리터럴도 동일한 방식으로 입력받음).

---

## 3. 문자형 (Character Types)

문자형은 단일 문자를 저장하며, 부호 있는(signed char)과 부호 없는(unsigned char)로 구분할 수 있습니다.

### 3.1. 부호 있는 문자 (Signed char)

- **리터럴 표현법:**
작은따옴표로 감싸서 사용
    - 예: `'A'`, `'z'`
- **출력 포맷:**
    - 문자 그대로: `%c`
        - 예:
            
            ```c
            signed char ch = 'A';
            printf("%c", ch);
            
            ```
            
    - 아스키 코드값 출력: `%d`
        - 예:
            
            ```c
            printf("%d", ch); // 'A' → 65
            
            ```
            
- **입력 포맷:**
    - 단일 문자 입력 시 `%c` 사용
        - 예:
            
            ```c
            signed char ch;
            scanf(" %c", &ch);  // 앞에 공백을 넣어 이전 입력의 개행 문자를 무시
            
            ```
            

### 3.2. 부호 없는 문자 (Unsigned char)

- **리터럴 표현법:**
리터럴 자체는 동일하게 작은따옴표로 사용
    - 예: `'A'`
- **출력 포맷:**
    - 문자 출력: `%c`
    - 아스키 코드값 출력: `%u`
        - 예:
            
            ```c
            unsigned char uch = 'A';
            printf("%c, %u", uch, uch);  // 문자와 아스키 값 출력
            
            ```
            
- **입력 포맷:**
    - `%c` 사용
        - 예:
            
            ```c
            unsigned char uch;
            scanf(" %c", &uch);
            
            ```
            

### 3.3. 이스케이프 시퀀스 (모든 char에 해당)

- **리터럴 표현법:**
특수 문자나 제어문자를 나타내기 위해 사용
    - 예:
        - 개행: `'\n'`
        - 탭: `'\t'`
        - 널 문자: `'\0'`
        - 벨: `'\a'`
        - 백스페이스: `'\b'`
        - 홑따옴표: `'\''`
        - 쌍따옴표: `\"`
        - 역슬래시: `\\`
- **출력 포맷:**`%c`
    - 예:
        
        ```c
        printf("%c", '\n');
        
        ```
        
- **입력 포맷:**`%c`를 사용하며, 필요에 따라 공백을 처리합니다.

---

## 4. 문자열 (String)

문자열은 문자 배열로 구성되며, 마지막에 널 문자 `'\0'`로 종료됩니다.

### 4.1. 문자열 상수

- **리터럴 표현법:**
큰따옴표로 감싸서 사용
    - 예: `"Hello, World!"`
- **출력 포맷:**`%s`
    - 예:
        
        ```c
        char str[] = "Hello, World!";
        printf("%s", str);
        
        ```
        
- **입력 포맷:**
    - `%s`를 사용하지만, 공백 문자가 포함된 문자열 입력 시에는 주의가 필요합니다.
    - 예:
        
        ```c
        char str[50];
        scanf("%49s", str);  // 공백 앞까지 입력 받음
        
        ```
        
    - 공백 포함 입력은 `fgets()`를 사용하는 것이 좋습니다.

---

`getchar()` 함수는 표준 입력(stdin)에서 단일 문자를 읽어올 때 사용됨

- **단일 문자 입력:** 사용자가 입력한 한 문자만 읽어올 때
    
    예: 사용자로부터 메뉴 선택을 받을 때
    
- **개행 문자 처리:** `scanf` 등으로 입력 후 남아있는 개행 문자(`'\n'`)를 소비할 때
    
    예: 여러 입력 후 버퍼를 정리하거나, `fgets()` 전후에 사용하여 불필요한 문자들을 제거
    
- **입력 대기:** 프로그램의 종료 전에 키 입력을 기다리는 용도로 사용
    
    예: "Press any key to continue..." 메시지 후 키 입력을 받음
