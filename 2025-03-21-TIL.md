# Oracle DB 연결하기
## 전체 연결 흐름

1. 환경 핸들 생성 (`OCIEnvCreate`)
2. 오류 핸들 생성 (`OCIHandleAlloc` with `OCI_HTYPE_ERROR`)
3. 서버 핸들 생성 및 연결 (`OCIHandleAlloc` + `OCIServerAttach`)
4. 서비스 컨텍스트 핸들 생성 (`OCIHandleAlloc`)
5. 서비스 컨텍스트에 서버 핸들 등록 (`OCIAttrSet`)
6. 사용자 세션 핸들 생성 (`OCIHandleAlloc`)
7. 사용자 이름 및 비밀번호 설정 (`OCIAttrSet`)
8. 세션 시작 (`OCISessionBegin`)
9. 세션 핸들을 서비스 컨텍스트에 등록 (`OCIAttrSet`)

---

## 단계별 설명

### 1) 환경 핸들 생성

```c
OCIEnvCreate(&envhp, OCI_DEFAULT, NULL, NULL, NULL, NULL, 0, NULL);

```

- OCI 라이브러리 사용을 위한 환경 초기화

### 2) 오류 핸들 생성

```c
OCIHandleAlloc(envhp, (dvoid**)&errhp, OCI_HTYPE_ERROR, 0, NULL);

```

- 이후 발생할 오류를 받아줄 핸들 생성

### 3) 서버 핸들 생성 및 연결

```c
OCIHandleAlloc(envhp, (dvoid**)&srvhp, OCI_HTYPE_SERVER, 0, NULL);
OCIServerAttach(srvhp, errhp, (text*)DBNAME, strlen(DBNAME), OCI_DEFAULT);

```

- DB에 대한 물리적 연결 수립

### 4) 서비스 컨텍스트 핸들 생성

```c
OCIHandleAlloc(envhp, (dvoid**)&svchp, OCI_HTYPE_SVCCTX, 0, NULL);

```

- 실제 SQL 실행을 위한 컨텍스트

### 5) 서비스 컨텍스트에 서버 핸들 등록

```c
OCIAttrSet(svchp, OCI_HTYPE_SVCCTX, srvhp, 0, OCI_ATTR_SERVER, errhp);

```

### 6) 세션 핸들 생성

```c
OCIHandleAlloc(envhp, (dvoid**)&usrhp, OCI_HTYPE_SESSION, 0, NULL);

```

### 7) 사용자 이름 및 비밀번호 등록

```c
OCIAttrSet(usrhp, OCI_HTYPE_SESSION, (void*)USERNAME, strlen(USERNAME), OCI_ATTR_USERNAME, errhp);
OCIAttrSet(usrhp, OCI_HTYPE_SESSION, (void*)PASSWORD, strlen(PASSWORD), OCI_ATTR_PASSWORD, errhp);

```

### 8) 세션 시작

```c
OCISessionBegin(svchp, errhp, usrhp, OCI_CRED_RDBMS, OCI_DEFAULT);

```

- Oracle 로그인 수행

### 9) 세션 핸들을 서비스 컨텍스트에 연결

```c
OCIAttrSet(svchp, OCI_HTYPE_SVCCTX, usrhp, 0, OCI_ATTR_SESSION, errhp);

```

- 로그인한 세션을 `svchp`와 연결하여 SQL 명령에 사용할 수 있도록 설정

---

## 연결 종료 시 처리

```c
OCILogoff(svchp, errhp);
OCIHandleFree((dvoid*)usrhp, OCI_HTYPE_SESSION);
OCIHandleFree((dvoid*)svchp, OCI_HTYPE_SVCCTX);
OCIHandleFree((dvoid*)srvhp, OCI_HTYPE_SERVER);
OCIHandleFree((dvoid*)errhp, OCI_HTYPE_ERROR);
OCIHandleFree((dvoid*)envhp, OCI_HTYPE_ENV);

```

- 메모리 해제 및 연결 종료
    
    ---
