## **C 프로그램의 메모리 영역**

| **메모리 영역** | **특징 및 역할** |
| --- | --- |
| **코드(Code) 영역** | - 프로그램의 실행 코드(기계어)가 저장되는 영역  - `main()` 및 기타 함수들이 위치  - 프로그램 실행 동안 유지됨 |
| **데이터(Data) 영역** | - **전역 변수, static 변수**가 저장됨 - 프로그램 시작 시 할당되며, 종료 시 해제됨 - 초기화된 변수(`.data`)와 초기화되지 않은 변수(`.bss`)로 나뉨 |
| **스택(Stack) 영역** | - **함수 호출 시 지역 변수 및 매개변수 저장**  - 후입선출 (LIFO) 구조 - 함수가 종료되면 자동으로 해제됨 |
| **힙(Heap) 영역** | - **동적 메모리 할당(malloc, calloc, realloc)**을 통해 관리 - `free()`를 호출해야 직접 해제 가능 - 프로그램 실행 중 크기를 동적으로 조정 가능 |

---

## **구조체 변수의 메모리 할당**

구조체 변수는 선언된 위치에 따라 **메모리 영역이 달라짐**

### **(1) 스택(Stack) 영역에 할당되는 구조체**

```c
#include <stdio.h>

struct book {
    char name[10];
    int age;
};

int main() {
    struct book sbv; // ✅ 스택 영역에 할당
}

```

✅ **분석**

- `sbv`는 `main()` 함수의 지역 변수이므로 **스택(Stack) 영역에 저장**됨.
- `main()`이 실행될 때 **스택 프레임**이 생성되며, 그 안에 `sbv`가 저장됨.
- `main()`이 종료되면 `sbv`는 **자동으로 해제됨**.

---

### **(2) 데이터(Data) 영역에 할당되는 구조체**

```c
struct book {
    char name[12];
    int age;
} b1, b2; // ✅ 전역 변수

```

✅ **분석**

- `b1`과 `b2`는 **전역 변수**이므로 **데이터(Data) 영역**에 저장됨.
- 프로그램이 시작될 때 할당되며, **프로그램 종료 시 해제됨**.

---

### **(3) 힙(Heap) 영역에 할당되는 구조체**

```c
#include <stdio.h>
#include <stdlib.h>

struct book {
    char name[12];
    int age;
};

int main() {
    struct book *b1 = (struct book*) malloc(sizeof(struct book)); // ✅ 힙 영역에 할당
    if (b1 == NULL) {
        printf("메모리 할당 실패\n");
        return 1;
    }

    free(b1); // ✅ 명시적으로 해제해야 함
}

```

✅ **분석**

- `malloc()`을 사용하여 `b1`을 **힙(Heap) 영역**에 할당.
- **동적 할당된 메모리는 `free()`를 사용하여 명시적으로 해제해야 함**.

---

## **전역 변수 vs 지역 변수 구조체 비교**

```c
#include <stdio.h>

struct p {
    char name[12];
    int age;
} b1, b2;  // ✅ 전역 변수 (데이터 영역에 저장)

int main() {
    printf("%p %p\n", &b1.name, &b2.age);
}

```

✅ **분석**

- `b1`과 `b2`는 **전역 변수**이므로 **데이터(Data) 영역**에 저장됨.
- 실행 중 **프로그램 종료 전까지 유지**됨.

```c
#include <stdio.h>

struct p {
    char name[12];
    int age;
};

int main() {
    struct p b1, b2; // ✅ 지역 변수 (스택 영역에 저장)
    printf("%p %p\n", &b1.name, &b2.age);
}

```

✅ **분석**

- `b1`, `b2`는 `main()` 함수 내부에서 선언된 **지역 변수**.
- **스택(Stack) 영역에 저장**되며, `main()` 종료 시 **자동 해제됨**.

---

## **4. `typedef`를 활용한 구조체 정의**

C에서는 `typedef`를 사용하여 **기존의 타입에 별칭을 부여**

### **(1) 기본적인 `typedef` 사용**

```c
typedef int Integer;
typedef float Real;

Integer a = 10;
Real b = 3.14;

```

### **(2) 구조체에서 `typedef` 사용**

```c
typedef struct book {
    char name[50];
    int year;
} Book;  // ✅ Book을 구조체의 별칭으로 정의

int main() {
    Book b1;  // 이제 struct book 대신 Book으로 선언 가능
}

```

✅ **장점**

1. `struct book` 대신 `Book`을 사용할 수 있어 **코드가 간결해짐**.
2. 유지보수성이 향상됨.

---

## **`typedef`의 활용 예제**

### **(1) 함수 포인터의 `typedef` 사용**

```c
typedef int (*func_ptr)(int, int);  // 함수 포인터 타입 정의

int add(int a, int b) {
    return a + b;
}

int main() {
    func_ptr f = add;  // 함수 포인터 사용
    printf("%d\n", f(3, 4));  // 7 출력
}

```

✅ **장점**

- 복잡한 **함수 포인터 선언을 단순화**할 수 있음.
    
    ---
