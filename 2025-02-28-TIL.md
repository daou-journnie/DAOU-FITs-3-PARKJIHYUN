### 🎯 **Mac에서 Oracle 21c 설치 및 사용 과정 (완벽 가이드)**

🚀 OS가 다른 환경에서 프로젝트를 개발하고 실행하기 위해 했던 시행착오 중 하나를 정리해보았습니다


---

## ✅ **1. 사전 준비**

### **1️⃣ Homebrew가 설치되어 있는지 확인**

```bash
brew --version

```

✅ 버전이 표시되면 **정상**

🚨 `command not found` 오류 발생 시, Homebrew 설치:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

```

---

## ✅ **2. Colima & Docker 설치 (Mac에서 Docker 환경 구성)**

Oracle DB는 Mac에서 직접 실행할 수 없기 때문에, **Colima(가벼운 가상 머신)** 위에서 Docker를 실행해야 합니다.

### **1️⃣ Colima 설치**

```bash
brew install colima

```

### **2️⃣ Docker 설치**

```bash
brew install docker
brew install docker-compose

```

### **3️⃣ Colima 실행**

M1/M2 Mac에서는 `x86_64` 아키텍처를 가상화해야 Oracle 21c가 실행됩니다.

```bash
colima start --memory 6 --arch x86_64

```

📌 **설명**

- `-memory 6` → 6GB RAM 할당 (Oracle은 최소 4GB 필요)
- `-arch x86_64` → M1/M2에서 `x86_64(AMD64)` 환경을 가상화

✅ **Colima가 정상 실행되었는지 확인**

```bash
colima status

```

🚀 **`arch: x86_64`라고 나오면 성공!**

---

## ✅ **3. Oracle 21c Docker 이미지 다운로드 & 실행**

### **1️⃣ Oracle 21c Docker 이미지 다운로드**

```bash
docker pull gvenzl/oracle-xe:21-full

```

✅ **약 2GB 다운로드 (네트워크 속도에 따라 다름)**

### **2️⃣ 컨테이너 실행 (`x86_64` 환경)**

```bash
docker run -d --name=oracle21c --restart=unless-stopped --platform linux/amd64 -p 1521:1521 -e ORACLE_PASSWORD=oracle1234 gvenzl/oracle-xe:21-full

```

📌 **설명**

- `-platform linux/amd64` → Mac(M1/M2)에서 `x86_64` 환경 강제 지정
- `p 1521:1521` → 로컬과 컨테이너의 포트 연결
- `e ORACLE_PASSWORD=oracle1234` → SYSTEM 계정 비밀번호 설정
- `-restart=unless-stopped` → Mac을 다시 켜면 자동 실행

✅ **컨테이너가 실행 중인지 확인**

```bash
docker ps

```

🚀 **결과 예시**

```
CONTAINER ID   IMAGE                      STATUS          PORTS                    NAMES
abc123456789   gvenzl/oracle-xe:21-full   Up 5 minutes    0.0.0.0:1521->1521/tcp   oracle21c

```

---

## ✅ **4. Oracle DB에 SYSTEM 계정으로 접속**

### **1️⃣ 컨테이너 내부에 접속**

```bash
docker exec -it oracle21c sqlplus / as sysdba

```

🚀 **정상적으로 접속되면 `SQL>` 프롬프트가 뜸.**

### **2️⃣ DB 상태 확인**

```sql
SELECT STATUS FROM V$INSTANCE;

```

✅ `OPEN`이면 정상 실행!

---

## ✅ **5. 새로운 사용자 계정 생성**

### **1️⃣ 사용자 계정 만들기**

```sql
CREATE USER mock_trade IDENTIFIED BY 1234;

```

📌 `mock_trade` → 원하는 사용자명

📌 `mock1234` → 원하는 비밀번호

### **2️⃣ 권한 부여**

```sql
GRANT CONNECT, RESOURCE TO mock_trade;

```

🚀 **이제 `mock_trade` 계정으로 로그인 가능!**

---

## ✅ **6. DataGrip에서 Oracle 연결**

### **1️⃣ DataGrip에서 새 연결 추가**

1. DataGrip 실행 → `+` 버튼 클릭 → **"Data Source" → "Oracle" 선택**
2. **아래 정보 입력**
    - **Host**: `localhost`
    - **Port**: `1521`
    - **User**: `mock_trade`
    - **Password**: `mock1234`
    - **Service Name**: `XEPDB1` (**⚠️ `SID`가 아니라 `Service Name` 사용!**)
3. `"Test Connection"` 클릭 → 연결 성공 확인
4. `"OK"` 버튼을 눌러 저장 후 접속!

🚀 **이제 DataGrip에서 SQL 실행 가능!**

---

## ✅ **7. 기본 테이블 생성 & 데이터 삽입**

### **1️⃣ 테이블 생성**

```sql
CREATE TABLE stocks (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    stock_name VARCHAR2(100) NOT NULL,
    stock_price NUMBER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

```

### **2️⃣ 데이터 삽입**

```sql
INSERT INTO stocks (stock_name, stock_price) VALUES ('Apple', 150);
INSERT INTO stocks (stock_name, stock_price) VALUES ('Tesla', 800);
INSERT INTO stocks (stock_name, stock_price) VALUES ('Amazon', 3300);
COMMIT;

```

### **3️⃣ 데이터 조회**

```sql
SELECT * FROM stocks;

```

🚀 **정상적으로 실행되면 데이터가 조회됨!**

---

## ✅ **8. 사용 후 정리 (Oracle DB 종료)**

### **1️⃣ Oracle DB 컨테이너만 중지**

```bash
docker stop oracle21c

```

📌 **다시 사용할 때**

```bash
docker start oracle21c

```

### **2️⃣ Docker & Colima까지 완전히 종료**

```bash
colima stop

```

📌 **다시 사용할 때**

```bash
colima start --memory 6 --arch x86_64`
docker start oracle21c

```

---

## 🎯 **최종 정리**

| 단계 | 명령어 |
| --- | --- |
| **1️⃣ Colima 실행** | `colima start --memory 6 --arch x86_64` |
| **2️⃣ Oracle 컨테이너 실행** | `docker run -d --name=oracle21c --restart=unless-stopped --platform linux/amd64 -p 1521:1521 -e ORACLE_PASSWORD=oracle1234 gvenzl/oracle-xe:21-full` |
| **3️⃣ SYSTEM 계정으로 접속** | `docker exec -it oracle21c sqlplus / as sysdba` |
| **4️⃣ 새 사용자 생성** | `CREATE USER mock_trade IDENTIFIED BY mock1234;` |
| **5️⃣ DataGrip 연결** | `localhost:1521`, `Service Name: XEPDB1` |
| **6️⃣ 사용 종료 (컨테이너 중지)** | `docker stop oracle21c` |
| **7️⃣ Docker & Colima 완전 종료** | `colima stop` |

---
