# CORS 에러
---

### 1. CORS 에러의 원인

- **동일 출처 정책 (Same-Origin Policy):**  
  웹 브라우저는 보안을 위해, 스크립트가 자신의 출처와 다른 출처의 리소스에 접근하는 것을 기본적으로 차단함
  예를 들어, `https://example.com`에서 로드된 웹 페이지가 `https://api.stockservice.com`의 데이터를 요청할 때, 이 두 도메인이 다르면 브라우저는 이를 위험하다고 판단함

- **서버의 CORS 설정 부재:**  
  요청하는 서버(API 서버)가 응답 헤더에 `Access-Control-Allow-Origin` 같은 CORS 관련 헤더를 포함하지 않으면, 브라우저는 이 응답을 차단함

---

### 2. 해결 방안

#### A. 서버측에서 CORS 헤더 설정

- **API 서버가 CORS를 지원하도록 수정:**  
  만약 API 서버를 직접 제어할 수 있다면, 서버 응답에 다음과 같은 헤더를 추가하여 모든 출처에서의 접근을 허용
  ```http
  Access-Control-Allow-Origin: *
  ```
  또는 특정 출처만 허용하고 싶다면, 예를 들어 `https://yourwebsite.com`:
  ```http
  Access-Control-Allow-Origin: https://yourwebsite.com
  ```
- **추가 헤더 설정:**  
  요청 메서드, 헤더 등이 다를 경우 추가 설정이 필요할 수 있습니다. 예를 들어, 프리플라이트(OPTIONS) 요청을 처리하고, 필요한 헤더를 명시할 수 있음
  ```http
  Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS
  Access-Control-Allow-Headers: Content-Type, Authorization
  ```

#### B. 클라이언트측 대안

- **프록시 서버 사용:**  
  만약 API 서버를 직접 수정할 수 없다면, 자신의 서버에서 API를 대신 호출하는 방법이 있음
  - 클라이언트는 자신의 서버에 요청을 보내고, 서버는 API 서버에 요청을 전달한 후, 결과를 클라이언트에 전달함
  이 방식은 동일 출처 정책 문제를 우회할 수 있음

- **JSONP 사용:**  
  만약 API가 JSONP를 지원한다면, `<script>` 태그를 이용해 데이터를 가져올 수 있음
  다만, JSONP는 GET 요청만 지원하며 보안상의 제약이 있으므로 주의

- **브라우저 확장 프로그램 (개발용):**  
  개발 중 테스트 목적으로 CORS 정책을 우회하는 브라우저 확장을 사용할 수 있음
  하지만, 이는 실제 서비스 환경에서는 사용할 수 없으므로 반드시 서버측 또는 프록시 서버를 통한 해결이 필

