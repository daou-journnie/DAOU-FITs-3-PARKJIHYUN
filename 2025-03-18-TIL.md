# 동적 메모리 할당
## `malloc` (Memory Allocation)

> 지정한 바이트 수만큼 연속된 메모리를 할당 → 시작 주소 반환
> 

`void* malloc(size_t size);`

- `size`: 할당받고자 하는 메모리의 바이트 크기
- return:
    - (성공) 할당된 메모리의 시작 주소
    - (실패)  `NULL` 반환
    
    **반환 값 검사하기!!!!**
    
    ```c
    int *p = malloc(n * sizeof(int));
    if (p == NULL) {
        // 메모리 할당 실패 처리
    }
    ```
    

1. 할당된 메모리를 **초기화하지 않음**
    
    ⇒ 쓰레기 값이 들어 있을 수 있음
    
    - 수동 초기화 하는 법
        
        `memset(p, 0, n * sizeof(int));`
        
2. **메모리 공간 연속 보장**
    - `size`만큼 연속된 공간을 운영체제로부터 할당받아 사용
3. **캐스팅**
    - C 언어에서 `void*`를 다른 포인터 타입에 자동으로 변환하기 때문에 별도 캐스팅이 필요 없음
    - 그러나 C++에서는 명시적 캐스팅(`(타입*)malloc(...)`)이 필요할 수 있습니다.
4. **반드시 `free`와 짝지어 사용**
    - 사용 후, `free(포인터)`로 해제
        - 해제하지 않을 경우 **메모리 누수(Leak)** 가 발생
    - 할당받은 메모리는 사용이 끝나면 `free`를 통해 해제하고, 포인터를 `NULL`로 설정

---

## `calloc` (Contiguous Allocation)

> `malloc`과 유사하게 동적 메모리를 할당하지만, 
**특정 요소 개수와 요소 크기를 인자로 받고, 할당된 메모리를 자동으로 0으로 초기화**
> 

`void* calloc(size_t nmemb, size_t size);`

- `nmemb`: 할당할 요소(Element)의 개수
- `size`: 각 요소의 크기(바이트)
- 최종적으로 `nmemb * size` 바이트를 할당한 뒤 모두 0으로 초기화
1. **자동 초기화**
    - 할당된 메모리 공간이 **바이트 단위**로 `0`(zero byte)으로 설정되어 있습니다.
    - 정수형, 실수형 변수가 모두 0으로 초기화되며, 구조체의 경우도 전체 바이트가 0으로 채워집니다.
2. **연속된 메모리 할당**
    - `nmemb` 개의 요소를 담을 공간을 연속으로 할당합니다(결국 내부적으로는 `nmemb * size` 만큼).
3. **안전성**
    - 0으로 초기화되어 있기 때문에, `malloc`에 비해 사용하기 조금 더 안전할 수 있습니다.
4. **인자 크기 조심**
    - `nmemb * size`가 매우 큰 값이 되어 **오버플로**가 일어날 수 있는지 점검해야 합니다.
5. **반환값 검사**
    - `malloc`과 마찬가지로 반환값이 `NULL`이 아닌지 항상 확인합니다.
6. **속도**
    - 0으로 초기화하기 때문에 `malloc`보다 약간 더 시간이 걸릴 수 있습니다.
    - 그러나 이는 할당 규모와 환경에 따라 달라질 수 있으며, 큰 차이가 아닐 수 있습니다.

---

## `realloc` (Re-Allocation)

> 이미 할당된 메모리 영역의 크기를 **재조정**(확대 혹은 축소)
> 

`void* realloc(void* ptr, size_t size);`

- `ptr`: 기존에 `malloc`, `calloc`, `realloc`로 **할당받은 메모리의 시작 주소**
- `size`: 새로 할당하고자 하는 크기(바이트)

1. `realloc`은 내부적으로 **새로운 메모리를 할당**한 뒤, 기존 메모리에 저장되어 있던 데이터를 **새로운 공간으로 복사**하고, 기존 메모리를 해제합니다.
2. 만약 **기존 메모리 영역을 확장할 수 있는 여유공간이 있다면**, 굳이 다른 곳으로 옮기지 않고 **인접 공간을 이어서** 쓰게 될 수도 있습니다. 이것은 구현체에 따라 다릅니다.
3. **줄이는 경우**, 일부는 잘려나갈 수 있으므로 주의가 필요합니다.
4. **복사 및 해제 동작**
    - `realloc`을 통해 용량을 늘리는 상황에서, 복사가 필요할 경우에는 **기존 메모리 내용이 전부 그대로 보장**됩니다(복사 후).
    - 그러나 **축소될 경우**, 초과분은 사라지므로, 잘려나갈 데이터가 없는지 신중히 고려해야 합니다.
5. **반환값**
    - `NULL`이 반환되면 재할당 실패입니다. 재할당 실패 시 **기존 메모리는 그대로 유효**합니다.
    - 재할당 성공 시 **새로운 주소**가 반환될 수 있으므로, `ptr`을 바로 덮어쓰면 **정보를 잃을 수 있음**에 주의해야 합니다.
        
        ```c
        // 바람직하지 않은 방식
        ptr = realloc(ptr, new_size);
        if (ptr == NULL) {
          // 할당 실패 시 ptr이 덮어써져서 기존 메모리 주소를 잃어버림.
        }
        
        ```
        
        ```c
        // 안전한 방식
        void *temp = realloc(ptr, new_size);
        if (temp == NULL) {
          // 실패 처리
        } else {
          ptr = temp; // 새 주소로 갱신
        }
        
        ```
        

### 구현 시 주의사항

1. **복사 비용**
    - 크기가 큰 배열을 재할당하면, 내부적으로 복사 비용이 발생할 수 있습니다.
2. **NULL 포인터 사용 가능**
    - `realloc(NULL, size)`는 `malloc(size)`와 동일하게 동작합니다.
3. **size가 0인 경우**
    - `realloc(ptr, 0)`는 `free(ptr)`와 유사하게 작동하고, 구현체에 따라 `NULL`을 반환할 수 있습니다.
    - 명시적으로 `free`를 부르는 것이 더 가독성이 좋습니다.
4. **실패 시 원본 포인터 유지**
    - 실패했을 때 `NULL`을 반환하므로, **원본 포인터는 잃어버리지 않도록** 별도의 임시 변수 사용이 권장됩니다.

---

## 함수 간 비교

| 함수명 | 파라미터 | 초기화 여부 | 동적 할당 방식 | 사용 예시 |
| --- | --- | --- | --- | --- |
| `malloc` | `malloc(size_t size)` | **X** (쓰레기 값) | 지정 바이트만큼 연속 메모리 할당 | 크기가 정해진 1차원 배열, 구조체 등 |
| `calloc` | `calloc(size_t nmemb, size_t size)` | **O** (0으로 초기화) | `nmemb*size` 바이트만큼 할당 후 0 | 2차원 배열, 구조체 배열 초기화에 유리 |
| `realloc` | `realloc(void* ptr, size_t size)` | 기존 내용 유지, 추가 부분은 보장 X | 기존 메모리 크기를 재조정 (확장/축소) | 동적 크기 변경이 필요한 배열, 버퍼 등을 재조정 |

### 주요 차이점

1. **초기화**:
    - `malloc`은 초깃값 없음(쓰레기 값).
    - `calloc`은 0으로 초기화.
    - `realloc`은 기존 메모리 내용을 최대한 보존하며, 새 영역 중 **새로 확장된 부분**은 반드시 초기화가 보장되지는 않음(구현체에 따라 다르므로 직접 초기화가 필요할 수 있음).
2. **사용 목적**:
    - `malloc`: 가장 일반적인 동적 할당.
    - `calloc`: 여러 개의 동일한 요소(배열 형태)에 대하여 0 초기화가 필요할 때.
    - `realloc`: 이미 할당된 메모리 공간을 유동적으로 늘리거나 줄이고 싶을 때.

---
