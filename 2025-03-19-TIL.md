# 비트마스크 정리

- 비트 마스크란 **특정 비트의 값을 설정, 확인, 변경하기 위해 사용되는 정수 값**
- `uint8_t flags` 변수는 **1바이트(8비트)** 크기의 `AccountStatus` 구조체에서 각 비트를 사용하여 계좌 상태를 저장


```c
#define ACTIVE_MASK        0x01  // 0000 0001 (1비트, 계좌 활성화)
#define VIP_MASK           0x02  // 0000 0010 (2비트, VIP 여부)
#define LOAN_MASK          0x04  // 0000 0100 (3비트, 대출 여부)
#define OVER_PROTECT_MASK  0x08  // 0000 1000 (4비트, 예금 보호 초과 여부)
#define BALANCE_MASK       0x70  // 0111 0000 (5~7비트, 잔액 상태 저장)
#define BALANCE_SHIFT 4          // 잔액 상태를 저장할 때 사용 (왼쪽 4비트 이동)

```

- `ACTIVE_MASK` (`0x01`) → **0000 0001** → 1번째 비트 사용
- `VIP_MASK` (`0x02`) → **0000 0010** → 2번째 비트 사용
- `LOAN_MASK` (`0x04`) → **0000 0100** → 3번째 비트 사용
- `OVER_PROTECT_MASK` (`0x08`) → **0000 1000** → 4번째 비트 사용
- `BALANCE_MASK` (`0x70`) → **0111 0000** → 5~7번째 비트 사용 (잔액 상태 저장)

---

 **`AccountStatus` 구조체**

```c
typedef struct {
    uint8_t flags;  // 1바이트(8비트)로 상태 저장
} AccountStatus;
 
```

- **`flags` 변수(8비트)를 사용하여 계좌의 모든 상태를 저장**
- *단일 바이트(1B)**로 여러 상태를 저장하여 **메모리 사용을 최적화**

---

**계좌 상태 설정 함수 (`setAccountStatus`)**

```c
void setAccountStatus(AccountStatus* account, int is_active, int is_vip, int has_loan, int over_protection, int balance_status) {
    account->flags = (is_active ? ACTIVE_MASK : 0) |
                     (is_vip ? VIP_MASK : 0) |
                     (has_loan ? LOAN_MASK : 0) |
                     (over_protection ? OVER_PROTECT_MASK : 0) |
                     ((balance_status << BALANCE_SHIFT) & BALANCE_MASK);
}

```


- `flags` 값을 비트 연산으로 설정하여, **각각의 상태를 1바이트 내에 저장**.
- `|` (비트 OR) 연산을 사용하여 **여러 상태를 하나의 변수에 결합**.
- `(balance_status << BALANCE_SHIFT) & BALANCE_MASK`
→ **잔액 상태(`balance_status`) 값을 비트 이동(shift) 후 저장**.

** `setAccountStatus(&customer1, 1, 1, 1, 1, 5);`**

- `is_active = 1` → `ACTIVE_MASK (0x01)`
- `is_vip = 1` → `VIP_MASK (0x02)`
- `has_loan = 1` → `LOAN_MASK (0x04)`
- `over_protection = 1` → `OVER_PROTECT_MASK (0x08)`
- `balance_status = 5`
    - `(5 << 4) = 0x50 (0101 0000)` (5를 4비트 왼쪽 이동)
    - `0x50 & 0x70 = 0x50`

 **최종 `flags` 값**

```
0000 0001  (ACTIVE_MASK)
0000 0010  (VIP_MASK)
0000 0100  (LOAN_MASK)
0000 1000  (OVER_PROTECT_MASK)
0101 0000  (BALANCE_MASK)
--------------
0101 1111  (최종 `flags` 값 = 0x5F)

```

이렇게 **1바이트(`flags`)에 모든 정보를 저장**

---

**계좌 상태 출력 함수 (`printAccountStatus`)**

```c
void printAccountStatus(AccountStatus account) {
    printf("계좌 상태:\n");
    printf(" - 활성화: %s\n", (account.flags & ACTIVE_MASK) ? "예" : "아니오");
    printf(" - VIP: %s\n", (account.flags & VIP_MASK) ? "예" : "아니오");
    printf(" - 대출 여부: %s\n", (account.flags & LOAN_MASK) ? "있음" : "없음");
    printf(" - 예금 보호 한도 초과: %s\n", (account.flags & OVER_PROTECT_MASK) ? "초과" : "초과하지 않음");

    const char* balance_labels[] = {
        "0원", "1~10만 원", "10~50만 원", "50~100만 원",
        "100~500만 원", "500~1000만 원", "1000~5000만 원", "5000만 원 이상"
    };
    int balance_status = (account.flags & BALANCE_MASK) >> BALANCE_SHIFT;
    printf(" - 잔액 상태: %s\n", balance_labels[balance_status]);
}

```


- `&` (비트 AND) 연산을 사용하여 각 상태를 확인.
- `BALANCE_MASK`와 `BALANCE_SHIFT`를 사용하여 잔액 상태 추출.

---

## **`main()` 실행**

```c
int main() {
    AccountStatus customer1;
    // 계좌 상태 설정 (활성화, VIP, 대출 있음, 보호 초과, 잔액 500~1000만 원)
    setAccountStatus(&customer1, 1, 1, 1, 1, 5);

    // 출력
    printAccountStatus(customer1);

    // 구조체 크기 확인
    printf("\nAccountStatus 구조체 크기: %lu 바이트\n", sizeof(AccountStatus));
    return 0;
}

```

- **1바이트(`flags`)로 모든 상태를 저장**.
- `printAccountStatus(customer1);`을 호출하여 현재 상태를 출력.

---


