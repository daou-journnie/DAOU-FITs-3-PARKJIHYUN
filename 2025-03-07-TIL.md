# 함수

## 함수의 종류

### **표준함수**

- 내장함수 또는 시스템 제공 함수라고 하며, 미리 정의되어 있어 헤더 파일에 포함
- 예: `printf()`, `scanf()`, `sin()`, `cos()` 등

### **사용자 정의 함수**

- 프로그래머가 특정 작업을 위해 직접 작성하는 함수
- 필요에 따라 정의하고 호출하여 사용

---

## 함수의 구조

C언어에서 함수를 작성할 때 기본적으로 3단계로 진행합니다.

1. **함수 원형 선언 (Prototype)**
    - 컴파일러에 함수의 반환타입과 매개변수 정보를 알려줍니다.
    
    ```c
    int avg(int, int);
    double sum(double, double, double);
    
    ```
    
2. **함수 호출 (Call)**
    - `main()` 함수 등에서 함수를 호출합니다.
    
    ```c
    double total = sum(50.8, 10.2, -12.35);
    int average = avg(100, 50);
    
    ```
    
3. **함수 정의 (Definition)**
    - 함수의 실제 구현을 작성합니다.
    
    ```c
    double sum(double i, double j, double k) {
        return i + j + k;
    }
    
    float avg(int i, int j) {
        return (i + j) / 2.0;
    }
    
    ```
    

*주의:* 매개변수를 **값으로 전달(Call by Value)** 하며, 함수 내부에서 변수의 복사본을 사용하게 됩니다.

---
