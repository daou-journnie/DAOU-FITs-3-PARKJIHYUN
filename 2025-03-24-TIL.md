# 모듈화(Modularization)

프로그램을 논리적으로 독립적인 기능 단위(모듈)로 나누어 개발, 유지보수, 재사용성을 극대화하는 소프트웨어 설계 기법

- 각 모듈은 **하나의 역할**에 집중
- 다른 모듈과 **낮은 결합도**와 **높은 응집도**

**장점**

- **유지보수성 향상:** 각 모듈이 독립적으로 작성·수정·테스트 가능
- **재사용성 증가:** 공통 기능 모듈을 다른 프로젝트에서도 재사용
- **협업 용이:** 여러 개발자가 동시에 서로 다른 모듈을 작업
- **컴파일 시간 단축:** 변경된 파일만 재컴파일하면 되어 빌드 속도 향상

**모듈화의 기준**

1. 한 모듈은 한 가지 책임(기능)만 수행 (단일 책임 원칙)
2. 인터페이스(함수 선언)와 구현(함수 정의)의 분리 (헤더파일 / 소스파일 분리)
3. **높은 응집도**와 **낮은 결합도** 유지
4. 재사용성 고려 – 공통 기능은 별도 모듈로 분리
5. 변경 가능성 고려 – 확장 가능하고 유지보수가 쉽게 설계

---

## C언어에서의 모듈화

- **헤더 파일(.h) + 소스 파일(.c)**
    - **헤더 파일(.h):** 함수 선언, 매크로, 전역 변수 extern 선언, 자료형 정의
    - **소스 파일(.c):** 함수 구현, 내부 로직 작성
- **독립적인 코드 관리**
    - 필요한 모듈만 수정하면 되므로 다른 모듈에 영향이 적음
    - 모듈 단위로 재사용 가능
- **규칙 / 관례**
    - **헤더 가드** 사용 (#ifndef ~ #define ~ #endif)
    - **전역 변수는 extern**으로 선언하여 필요한 파일에서만 참조
    - **static 키워드**로 내부 전역 변수와 함수 숨기기
    - main.c에는 가능한 한 프로그램 진입점만 두고, 나머지 로직은 분리
- **전역 static 변수**
    - 다른 파일에 동일한 이름의 변수가 있더라도 충돌 방지
    - 외부 접근이 막히므로 모듈 내부에서만 사용 가능 (캡슐화)
- **소스 파일 분할 규칙**
    - main.c: 프로그램 진입점 (다른 함수, 구조체, 변수는 가급적 두지 않음)
    - 소스 파일별로 역할 분리 (예: bank.c, db.c, ui.c, …)
    - 각 파일의 전역 변수에는 static을 붙여 모듈 내부로 숨김
    - 모듈 간 의존성을 낮추고, 상호 참조를 최소화

---

## 실습 예제

`account.h`

```c
#pragma once

#ifndef ACCOUNT_H
#define ACCOUNT_H

// 구조체 선언
typedef struct {
    int accountNumber;
    double balance;
} Account;

// 함수 선언
void createAccount(Account* account, int accountNumber, double initialBalance);
void deposit(Account* account, double amount);
void withdraw(Account* account, double amount);
void printAccountInfo(Account* account);

#endif //ACCOUNT_H

```

`account.c`

```c
#include <stdio.h>
#include "account.h"

void createAccount(Account* account, int accountNumber, double initialBalance) {
    account->accountNumber = accountNumber;
    account->balance = initialBalance;
}

void deposit(Account* account, double amount) {
    if (amount < 0) {
        printf("Invalid amount\n");
        return;
    }
    account->balance += amount;
}

void withdraw(Account* account, double amount) {
    if (amount < 0 || account->balance < amount) {
        printf("Invalid amount\n");
        return;
    }
    account->balance -= amount;
}

void printAccountInfo(Account* account) {
    printf("Account Number: %d\n", account->accountNumber);
    printf("Balance: %.2f\n", account->balance);
}

```

`main.c`

```c
#include <stdio.h>
#include "account.h"
int main() {
    Account myAccount;

    createAccount(&myAccount, 1001, 500.0);
    deposit(&myAccount, 200.0);
    withdraw(&myAccount, 100.0);

    printAccountInfo(&myAccount);
    return 0;
}

```
