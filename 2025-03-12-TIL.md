### 메모리 구조

- 코드 영역
    - 함수, 명령어 읽기 전용
- 데이터 영역
    - 전역변수, 정적변수
- 스택영역
    - 지역변수, 매개변수, 바놘
- 힙 영역
    - 동적할당

# 2차원 배열 포인터

```
	메모리 주소:  0x100   0x104   0x108   0x10C   0x110   0x114   0x118   0x11C   0x120
              ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
배열 m:       │m[0][0]││m[0][1]││m[0][2]││m[1][0]││m[1][1]││m[1][2]││m[2][0]││m[2][1]││m[2][2]│
              └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘

```

여기서 각 `int`는 4바이트라고 가정합니다.

### 포인터 산술의 예

1. ***(m+1) → &m[1][0]**
    - `m`은 행렬의 시작 주소, 즉 &m[0][0] (0x100)입니다.
    - `m+1`는 한 행(3개의 int, 12바이트) 뒤로 이동한 주소 → 0x100 + 12 = 0x10C.
    - `(m+1)`는 이 주소에 저장된 배열(즉, m[1])의 첫 번째 원소, &m[1][0] (0x10C)를 가리킵니다.
2. ***(m+1)+1 → &m[1][1]**
    - 위에서 *(m+1)는 &m[1][0] (0x10C)입니다.
    - 여기에 +1은 int 한 개 크기(4바이트)만큼 이동 → 0x10C + 4 = 0x110.
    - 따라서 *(m+1)+1은 &m[1][1] (0x110)를 가리킵니다.
3. ***(m+1+1) 또는 (m+2) → &m[2][0]*
    - `m+1+1`은 m+2로, 시작 주소 0x100에서 2행(2×12바이트 = 24바이트) 뒤 → 0x100 + 24 = 0x118.
    - `(m+2)`는 이 주소의 배열, 즉 m[2]의 첫 번째 원소, &m[2][0] (0x118)를 가리킵니다.

---

### 요약 다이어그램

```
m[0] row:   m[0][0]   m[0][1]   m[0][2]
              0x100     0x104     0x108

m[1] row:   m[1][0]   m[1][1]   m[1][2]
              0x10C     0x110     0x114

m[2] row:   m[2][0]   m[2][1]   m[2][2]
              0x118     0x11C     0x120

```

- ***(m+1)**
    
    → m+1 = 0x10C, *(m+1) = &m[1][0].
    
- ***(m+1)+1**
    
    → *(m+1) = 0x10C, +1 int(4바이트) → 0x110, 즉 &m[1][1].
    
- ***(m+2)**
    
    → m+2 = 0x118, *(m+2) = &m[2][0].
    

이와 같이 포인터 산술을 사용하면, 행과 열의 위치에 따라 원하는 요소의 주소에 쉽게 접근할 수 있습니다.

# 포인터 배열 (pointer array)

> 포인터들을 요소로 갖는 배열
> 
- 각 배열 요소가 특정 데이터의 주소를 저장하는 포인터

### **선언:**

- 예를 들어, 정수형 변수들을 가리키는 포인터 배열은 다음과 같이 선언할 수 있습니다.

이는 5개의 int형 포인터를 저장하는 배열을 의미합니다.
    
    ```c
    int *arr[5];
    
    ```
    

### **용도:**

- 여러 개의 변수나 문자열, 혹은 동적으로 할당된 메모리의 주소들을 한 곳에 모아서 관리할 때 사용합니다.
- 예를 들어, 문자열 배열(문자열을 가리키는 포인터 배열)을 선언할 때 많이 사용합니다.

여기서 `names`는 문자열의 시작 주소를 저장하는 포인터들의 배열입니다.
    
    ```c
    char *names[] = {"Alice", "Bob", "Charlie"};
    
    ```
    

### **메모리 접근:**

- 포인터 배열의 각 요소에 저장된 주소를 통해 해당 메모리에 접근할 수 있습니다.
    
    ```c
    printf("%s\n", names[1]); // "Bob" 출력
    
    ```
    
- 배열의 인덱싱을 통해 각 포인터를 선택하고, 다시 역참조하여 그 값에 접근할 수 있습니다.

### **동적 할당과 결합:**

- 포인터 배열은 동적으로 할당된 메모리를 관리할 때도 유용합니다. 예를 들어, 동적 문자열 배열이나 2차원 배열 등을 구현할 때 사용합니다.
    
    ```c
    int *pArr[3];
    for (int i = 0; i < 3; i++) {
        pArr[i] = (int*)malloc(5 * sizeof(int)); // 5개의 int를 저장할 수 있는 메모리 할당
    }
    // 사용 후, 각 요소에 대해 free(pArr[i]) 호출 필요
    
    ```
    

### **포인터 배열과 포인터:**

- 포인터 배열과 단일 포인터는 개념이 다릅니다.
    - **포인터 배열:** 여러 개의 포인터들을 저장하는 배열입니다.
    예: `int *arr[5];` → 5개의 int형 포인터 저장
    - **포인터:** 단일 변수의 주소를 저장하는 변수입니다.
    예: `int *p;`

### 예제 코드

아래는 문자열을 저장하는 포인터 배열의 예제입니다.

```c
#include <stdio.h>

int main(void) {
    // 문자열의 주소들을 저장하는 포인터 배열 선언
    char *names[] = {"Alice", "Bob", "Charlie", "Dave"};
    int numNames = sizeof(names) / sizeof(names[0]);

    // 각 문자열 출력
    for (int i = 0; i < numNames; i++) {
        printf("names[%d] = %s\n", i, names[i]);
    }

    return 0;
}

```

- **설명:**
    - `char *names[]`는 문자열(문자 배열의 시작 주소)을 저장하는 포인터 배열입니다.
    - 각 요소 `names[i]`는 문자열의 시작 주소를 갖고 있으므로, `%s` 포맷으로 출력하면 해당 문자열이 출력됩니다.
    - `sizeof(names) / sizeof(names[0])`는 배열에 저장된 포인터의 개수를 계산하는 방법입니다.

---

<aside>
✨

## 포인터 배열과 2차원 배열

---

### 방법 A: 개별 문자열 배열과 포인터 배열 사용

```c
char str1[MAX_LEN], str2[MAX_LEN], str3[MAX_LEN];
char* arr[SIZE] = { str1, str2, str3 }; // 문자열 포인터 배열
char** ptr = arr; // 이중 포인터

```

- **개별 배열 선언:**
    - `str1`, `str2`, `str3`는 각각 별도의 1차원 문자 배열입니다.
    - 이들은 각각 MAX_LEN 크기의 버퍼를 갖고, 메모리 상에서는 독립적으로 할당됩니다.
- **포인터 배열:**
    - `arr`는 3개의 포인터를 담는 배열로, 각각 `str1`, `str2`, `str3`의 시작 주소를 저장합니다.
    - 즉, `arr[0]`은 `str1`의 주소, `arr[1]`은 `str2`의 주소, `arr[2]`은 `str3`의 주소를 가집니다.
- **이중 포인터:**
    - `ptr`는 `arr` 배열의 시작 주소를 가리키므로, `ptr[i]`를 사용하면 `str1`, `str2`, `str3` 중 i번째 문자열의 시작 주소에 접근할 수 있습니다.
        - `ptr[1]`는 `str2`의 시작 주소를 가리키므로,
        - `ptr[1] + 1`은 포인터 산술에 의해 `str2`의 두 번째 요소(문자)의 주소를 가리킵니다.
        
        예를 들어,
        
        ```c
        char str2[MAX_LEN] = "HELLO";
        
        ```
        
        라면,
        
        - `ptr[1]`는 "HELLO"의 'H'를 가리키고,
        - `ptr[1] + 1`은 'E'를 가리키게 됩니다.

**장점 및 유연성**

- **재할당 및 수정 용이:**
    - 포인터 배열의 각 요소는 독립적으로 재할당할 수 있으므로, 필요에 따라 다른 문자열의 주소로 변경할 수 있습니다.
- **동적 메모리와 결합:**
    - 만약 문자열들을 동적으로 할당할 경우에도 포인터 배열로 관리하기 쉽습니다.

**메모리 배치**

- 각 문자열은 독립적인 메모리 블록에 존재합니다. (반드시 연속적이지 않을 수 있음)
- 포인터 배열(`arr`)은 별도의 메모리 영역에 할당되며, 각 요소에 개별 문자열의 주소가 저장됩니다.

---

### 방법 B: 2차원 배열 사용

```c
char str[3][100];
int lenRank[3];

```

- **2차원 배열 선언:**
    - `str`은 3개의 행과 100개의 열을 가진 2차원 문자 배열입니다.
    - 메모리 상에서는 3×100 크기의 연속된 블록으로 할당됩니다.
- **접근 방식:**
    - `str[i]`는 i번째 행의 첫 번째 문자의 주소를 나타내며, 이는 `(str + i)`와 동일합니다.
    - 각 행의 요소는 `str[i][j]` 또는 `(*(str + i) + j)`로 접근할 수 있습니다.

**장점 및 특성**

- **메모리 연속성:**
    - 모든 문자열이 한 블록의 연속된 메모리에 저장되므로, 캐시 효율이 높을 수 있습니다.
- **고정 크기:**
    - 모든 행의 크기가 동일하며, 배열 전체 크기가 컴파일 시 결정됩니다.
- **재할당 제한:**
    - 2차원 배열의 각 행은 고정된 메모리 블록이므로, 특정 행의 주소를 다른 곳으로 재할당할 수 없습니다.

---

**비교 요약**

| 구분 | 방법 A (포인터 배열) | 방법 B (2차원 배열) |
| --- | --- | --- |
| **선언 방식** | 개별 문자열 배열 `str1, str2, str3` + 포인터 배열 `arr[]` | 2차원 배열 `str[3][100]` |
| **메모리 배치** | 각 문자열은 독립적인 메모리 블록에 할당됨, 포인터 배열은 별도 | 전체 문자열이 연속된 메모리 블록에 저장됨 |
| **접근 방식** | `ptr[i]` 또는 `arr[i]`를 사용하여 각 문자열에 접근 | `str[i]` 또는 `*(str+i)`로 각 행(문자열)에 접근 |
| **유연성** | 각 포인터 요소를 재할당할 수 있어 동적 할당 및 수정 용이 | 고정된 메모리 구조로 재할당이 어려움 |
| **메모리 관리** | 동적 메모리 할당과 결합하여 유연하게 관리 가능 | 컴파일 시 결정된 크기로 관리, 크기 변경 불가능 |

---

- **방법 A (포인터 배열):**
    - 독립적인 문자열 배열들을 포인터 배열로 관리하여, 각 문자열의 주소를 유연하게 다룰 수 있습니다.
    - 동적 메모리 할당이나 문자열 재배치가 필요할 때 유리합니다.
- **방법 B (2차원 배열):**
    - 모든 문자열이 하나의 연속된 메모리 블록에 저장되므로, 메모리 연속성이 보장됩니다.
    - 고정 크기 데이터 처리에 적합하며, 인덱스를 통한 접근이 간단합니다.

각 방법은 사용 목적과 필요에 따라 선택할 수 있습니다. 동적 할당, 재할당, 또는 다양한 문자열의 관리가 필요하다면 방법 A가, 고정 크기의 데이터를 효율적으로 처리하고 싶다면 방법 B가 적합합니다.

</aside>

## 예시 코드

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <ctype.h>

#define SIZE 3         // 입력받을 문자열의 개수
#define MAX_LEN 100    // 각 문자열의 최대 길이

// ------------------------------------------------------
// 함수: to_uppercase
// 목적: 전달된 문자열을 모두 대문자로 변환한다.
// 인자: char* str - 변환할 문자열의 포인터
// ------------------------------------------------------
void to_uppercase(char* str) {
    // 포인터 산술을 이용하여 문자열의 각 문자를 순회하며 변환
    while (*str) {              // *str가 널('\0')가 아닐 때까지
        *str = toupper(*str);   // 현재 문자를 대문자로 변환하여 저장
        str++;                  // 다음 문자로 포인터 이동
    }
}

// ------------------------------------------------------
// 함수: sort_by_length
// 목적: 문자열 포인터 배열을 문자열 길이 기준으로 오름차순 정렬한다.
// 인자: char* arr[] - 문자열 포인터 배열
//       int n       - 배열의 크기
// ------------------------------------------------------
void sort_by_length(char* arr[], int n) {
    // 전형적인 버블 정렬 알고리즘을 이용하여 인접한 두 문자열 길이를 비교
    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < n - 1 - i; j++) {
            // strcmp 대신 문자열 길이 비교: strlen(arr[j])와 strlen(arr[j+1])
            if (strlen(arr[j]) > strlen(arr[j + 1])) {
                // swap: 두 포인터 값을 교환하여 배열 내 순서를 변경
                char* temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
}

int main() {
    // 세 개의 문자열을 저장할 배열들을 각각 선언
    char str1[MAX_LEN], str2[MAX_LEN], str3[MAX_LEN];

    // 포인터 배열에 각 문자열 배열의 주소를 저장하여, 문자열들을 쉽게 관리
    char* arr[SIZE] = { str1, str2, str3 };

    // 이중 포인터 ptr은 arr의 주소를 가리키며, 포인터 배열로도 사용할 수 있다.
    // 여기서는 ptr과 arr는 동일한 역할을 하므로, 편의상 arr를 사용함.
    // char** ptr = arr; // (원하는 경우 사용할 수 있음)

    // 문자열 입력
    printf("문자열 3개 입력:\n");
    for (int i = 0; i < SIZE; i++) {
        // 안전하게 문자열 입력 (fgets 대신 scanf 사용: 공백이 없는 문자열)
        // 공백이 포함된 문자열을 입력받으려면 fgets 사용 후 개행 문자 제거 필요.
        scanf("%s", arr[i]);
    }

    // 모든 문자열을 대문자로 변환 (모듈화된 함수 호출)
    for (int i = 0; i < SIZE; i++) {
        to_uppercase(arr[i]);
    }

    // 대문자로 변환된 문자열 출력
    printf("\n대문자로 변환된 문자열:\n");
    for (int i = 0; i < SIZE; i++) {
        printf("%s\n", arr[i]);
    }

    // 문자열 길이순 정렬: 버블 정렬 함수를 이용하여 arr 배열 자체를 정렬
    sort_by_length(arr, SIZE);

    // 정렬된 문자열 출력
    printf("\n문자열 길이순 정렬 (오름차순):\n");
    for (int i = 0; i < SIZE; i++) {
        printf("%s\n", arr[i]);
    }

    return 0;
}

```

---

### 중요한 개념 정리

1. **포인터 배열 (Array of Pointers)**
    - 여러 개의 포인터를 저장하는 배열입니다.
    - 예: `char* arr[SIZE];`는 SIZE개의 문자열의 시작 주소를 저장할 수 있습니다.
    - 여러 문자열을 한 번에 관리하고, 정렬(교환) 등에서 각 문자열의 주소를 쉽게 변경할 수 있습니다.
2. **이중 포인터 (Double Pointer)**
    - 포인터의 주소를 저장하는 변수입니다.
    - 예: `char** ptr = arr;`와 같이 사용하면, ptr은 문자열 포인터 배열의 주소를 참조할 수 있습니다.
    - 함수에서 포인터 배열을 매개변수로 전달하거나, 동적 2차원 배열을 구현할 때 사용합니다.
3. **문자열과 배열의 초기화**
    - 문자열은 문자 배열로 저장되며, 컴파일러는 자동으로 널 종료 문자(`'\0'`)를 추가합니다.
    - 배열을 초기화할 때 크기를 명시하지 않아도, 초기화 값의 개수에 따라 크기가 결정됩니다.
        
        ```c
        char str[] = "EXAMPLE"; // 배열의 크기는 8(7글자 + '\0')
        
        ```
        
4. **포인터 산술**
    - 포인터에 정수를 더하면 해당 포인터가 가리키는 자료형 크기만큼 주소가 이동
    - 예: `(str + i)`는 `str[i]`와 동일합니다.
    - 배열의 특정 요소에 접근할 때 주로 사용
5. **함수 포인터 및 함수 호출**
    - **포함되지는 않았지만 중요한 개념:**
        - 함수 포인터는 함수의 주소를 저장하는 포인터로, 동적 함수 호출 및 콜백 함수 구현에 사
6. **정렬 알고리즘 (버블 정렬)**
    - 인접한 두 요소를 반복적으로 비교하고 필요시 교환하여 배열 전체를 정렬하는 알고리즘
    - 시간 복잡도는 O(n²)이지만, 구현이 간단하여 소규모 데이터에서는 유용
    - **구현:**
        
        ```c
        void sort_by_length(char* arr[], int n) {
            for (int i = 0; i < n - 1; i++) {
                for (int j = 0; j < n - 1 - i; j++) {
                    if (strlen(arr[j]) > strlen(arr[j + 1])) {
                        char* temp = arr[j];
                        arr[j] = arr[j + 1];
                        arr[j + 1] = temp;
                    }
                }
            }
        }
        
        ```
        

---

# 함수 포인터

### 함수 포인터의 기본 개념

- **정의:**
    
    함수 포인터는 함수의 시작 주소(즉, 함수의 위치)를 저장하는 포인터
    
    함수 이름은 사용될 때 함수의 주소로 변환되므로, 이를 포인터에 저장
    
- **예시:**
    
    ```c
    int add(int a, int b) {
        return a + b;
    }
    
    ```
    
    이때, `add`는 함수의 이름으로, 호출 시 `add(10, 20)`과 같이 사용되지만, 주소로 변환되어 포인터에 저장
    

---

### 함수 포인터의 선언과 초기화

```c
반환형 (*포인터이름)(매개변수목록);

```

```c
int (*funcPtr)(int, int);

```

- **초기화:**
    
    함수 포인터에 함수의 주소를 할당할 때는 함수 이름을 그대로 대입하면 됩니다.
    
    ```c
    funcPtr = add;  // 또는 funcPtr = &add; (두 가지 모두 사용 가능)
    
    ```
    
- **사용:**
    
    함수 포인터를 사용하여 함수를 호출할 수 있습니다.
    
    ```c
    int result = funcPtr(10, 20); // add(10, 20)과 동일하게 호출됨
    
    ```
    

---

### 함수 포인터의 응용

### (1) 콜백 함수

- **목적:**
함수 포인터를 매개변수로 전달하여, 함수 내에서 동적으로 실행할 함수를 선택할 수 있습니다.
- **예제:**
→ 여기서 `calculate` 함수는 전달받은 함수 포인터를 사용하여, 어떤 연산을 수행할지 동적으로 결정합니다.
    
    ```c
    #include <stdio.h>
    
    // 두 개의 정수를 더하는 함수
    int add(int a, int b) {
        return a + b;
    }
    
    // 두 개의 정수를 빼는 함수
    int subtract(int a, int b) {
        return a - b;
    }
    
    // 함수 포인터를 매개변수로 받아서, 해당 함수를 호출하는 함수
    void calculate(int x, int y, int (*operation)(int, int)) {
        printf("결과: %d\n", operation(x, y));
    }
    
    int main() {
        calculate(10, 5, add);      // add 함수가 호출되어 10+5=15 출력
        calculate(10, 5, subtract); // subtract 함수가 호출되어 10-5=5 출력
        return 0;
    }
    
    ```
    

### (2) 함수 포인터 배열

- **목적:**
여러 함수를 저장하는 배열을 만들어 필요할 때마다 다른 함수를 호출할 수 있습니다.
- **예제:**
    
    ```c
    #include <stdio.h>
    
    int add(int a, int b) { return a + b; }
    int subtract(int a, int b) { return a - b; }
    int multiply(int a, int b) { return a * b; }
    
    int main() {
        // 함수 포인터 배열 선언 및 초기화
        int (*operations[3])(int, int) = { add, subtract, multiply };
    
        // 배열의 각 함수 호출
        printf("10 + 5 = %d\n", operations[0](10, 5));      // add 호출
        printf("10 - 5 = %d\n", operations[1](10, 5));      // subtract 호출
        printf("10 * 5 = %d\n", operations[2](10, 5));      // multiply 호출
    
        return 0;
    }
    
    ```
    

### (3) typedef를 이용한 함수 포인터 선언

- **목적:**
함수 포인터 선언이 길어질 수 있으므로, `typedef`를 사용하여 보다 간단하게 선언할 수 있습니다.
- **예제:**
→ 이렇게 하면 함수 포인터 타입을 `Operation`으로 정의하여 코드의 가독성을 높일 수 있습니다.
    
    ```c
    #include <stdio.h>
    
    // 함수 포인터 타입을 Operation으로 정의 (두 int를 받아 int를 반환)
    typedef int (*Operation)(int, int);
    
    int add(int a, int b) { return a + b; }
    int multiply(int a, int b) { return a * b; }
    
    // Operation 타입의 함수 포인터를 매개변수로 사용
    void execute(Operation op, int x, int y) {
        printf("결과: %d\n", op(x, y));
    }
    
    int main() {
        execute(add, 10, 20);      // add 호출 → 30
        execute(multiply, 5, 4);   // multiply 호출 → 20
        return 0;
    }
    
    ```
    

---

### 요약

- **함수 포인터 선언:**
    - 기본 형식: `반환형 (*포인터이름)(매개변수목록);`
- **함수 포인터의 초기화:**
    - 함수 이름을 대입하여 주소를 저장 (예: `funcPtr = add;`)
- **함수 호출:**
    - 함수 포인터를 사용하여 함수를 호출 (예: `funcPtr(10, 20)`)
- **응용:**
    - 콜백 함수, 동적 함수 호출, 함수 포인터 배열, typedef를 통한 선언 간소화 등 다양한 용도로 사용

함수 포인터를 활용하면 프로그램의 유연성을 높이고, 동적으로 함수의 선택과 호출이 가능해지며, 이벤트 기반 프로그래밍이나 다형성을 구현할 때 중요한 역할을 합니다.
