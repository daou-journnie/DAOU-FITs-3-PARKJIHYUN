## 포인터가 왜 필요한지

<aside>
✨

컴퓨터가 변수에 저장된 값을 출력하는 법!

---

1. **컴파일 및 실행 파일 생성:**
    - 소스 코드는 기계어로 번역되어 실행 파일(.exe)로 저장
2. **메모리 적재:**
    - 로더가 실행 파일을 메모리에 적재하여 코드 영역, 데이터 영역, 스택 등을 구성
3. **심벌 테이블:**
    - 컴파일러는 변수명, 함수명 등 심벌 테이블을 생성하며, 각 변수의 메모리 주소 정보를 포함
4. **실행 시 변수 접근:**
    - 프로그램이 실행될 때, 심벌 테이블의 주소 정보를 통해 변수에 접근하고, 해당 값을 읽어 화면에 출력


</aside>

### 포인터 역할
1. **메모리 직접 접근:**
    - 포인터를 사용하면 변수의 메모리 주소에 직접 접근할 수 있습니다.
    - 이는 변수의 값을 읽거나 수정할 때, 메모리의 특정 위치를 정확하게 지정할 수 있게 해줍니다.
    - 예를 들어, `printf("%d", *p);`와 같이 포인터를 역참조하면 그 주소에 저장된 값을 가져올 수 있습니다.
2. **효율적인 데이터 전달 (Call by Reference):**
    - 포인터를 함수 매개변수로 전달하면, 값 복사 없이 변수의 주소만 전달되므로 메모리와 시간 자원을 절약할 수 있습니다.
    - 또한 함수 내부에서 변수의 값을 변경할 수 있으므로, 여러 함수 간에 데이터를 공유하거나 수정할 때 매우 유용합니다.
    - 예시로, `swap(&a, &b);`는 두 변수의 주소를 전달하여 함수 내부에서 직접 값을 교환할 수 있습니다.
3. **동적 메모리 할당:**
    - 실행 중 필요한 메모리를 동적으로 할당하고, 해제하는 작업은 포인터를 통해 이루어집니다.
    - `malloc()`, `free()`와 같은 함수는 포인터를 사용하여 동적 메모리를 관리합니다.
    - 이를 통해 프로그램은 메모리 사용을 최적화하고, 실행 시간에 필요한 자원을 유연하게 할당할 수 있습니다.
4. **데이터 구조와 복잡한 배열 처리:**
    - 포인터를 이용하면 2차원 배열, 연결 리스트, 트리와 같은 복잡한 데이터 구조를 효과적으로 구현할 수 있습니다.
    - 이중 포인터는 동적 2차원 배열을 만들거나, 함수 내부에서 포인터의 주소를 변경하여 새로운 메모리 영역을 참조하도록 할 때 사용됩니다.
5. **함수 포인터 및 콜백 함수 구현:**
    - 포인터는 함수의 주소를 저장할 수 있으므로, 함수 포인터를 이용해 동적으로 실행할 함수를 선택할 수 있습니다.
    - 이는 콜백(callback) 함수를 구현하거나, 이벤트 기반 프로그래밍에서 매우 중요한 역할을 합니다.
6. **심벌 테이블과 변수 관리:**
    - 컴파일러가 생성한 심벌 테이블 덕분에 변수 이름과 그에 대응하는 메모리 주소가 연결됩니다.
    - 포인터는 이러한 주소 정보를 활용하여, 실행 시 변수에 접근할 수 있게 하며, 프로그램이 변수의 값을 읽고 출력하는 핵심 수단이 됩니다.

---

## 포인터의 기본 개념

포인터는 다른 변수의 **메모리 주소**를 저장하는 변수

- `&` 연산자를 사용하여 변수의 주소를 얻습니다.
    - 연산자(역참조)를 사용하여 포인터가 가리키는 주소에 저장된 값을 가져옵니다.

```c
#include <stdio.h>
int main() {
    int a = 10;      // 정수형 변수
    int *p = &a;     // 변수 a의 주소를 저장하는 포인터 변수
    printf("변수 a의 값: %d\n", a);
    printf("변수 a의 주소: %p\n", (void*)&a);
    printf("포인터 p가 가리키는 주소: %p\n", (void*)p);
    printf("포인터 p가 가리키는 값: %d\n", *p); // 역참조
    return 0;
}

```

---

## 포인터와 배열

배열의 이름은 배열의 첫 번째 원소의 주소를 나타냅니다.

- `arr[i]`는 내부적으로 `(arr + i)`와 동일한 의미

```c
#include <stdio.h>
int main() {
    int arr[3] = {1, 2, 3};
    int *p = arr; // 배열 이름은 첫 번째 원소의 주소
    printf("arr[0] = %d, *p = %d\n", arr[0], *p);
    printf("arr[1] = %d, *(p+1) = %d\n", arr[1], *(p+1));
    printf("arr[2] = %d, *(p+2) = %d\n", arr[2], *(p+2));
    return 0;
}

```

---

## 포인터와 동적 메모리 할당

프로그램 실행 중에 필요한 메모리를 **동적으로 할당**하고, 사용 후에 해제하여 메모리 관리를 효율적

- `malloc()`로 메모리를 할당하고, `free()`로 해제

```c
#include <stdio.h>
#include <stdlib.h>
int main() {
    int *p = (int*)malloc(sizeof(int)); // 정수형 메모리 할당
    if (p == NULL) {
        printf("메모리 할당 실패\n");
        return 1;
    }
    *p = 42; // 할당된 메모리에 값 저장
    printf("동적 할당된 메모리의 값: %d\n", *p);
    free(p); // 메모리 해제
    return 0;
}

```

---

## 포인터와 함수 (Call by Reference)

포인터를 함수에 전달하면, **함수 내부에서 변수의 값을 직접 변경**할 수 있습니다.

```c
#include <stdio.h>
void swap(int *x, int *y) {
    int temp = *x;
    *x = *y;
    *y = temp;
}
int main() {
    int a = 5, b = 10;
    printf("교환 전: a = %d, b = %d\n", a, b);
    swap(&a, &b); // 변수의 주소를 전달 (Call by Reference)
    printf("교환 후: a = %d, b = %d\n", a, b);
    return 0;
}

```

---

## 이중 포인터 (Double Pointer, **)

이중 포인터는 **포인터를 가리키는 포인터**입니다.

- 즉, 변수의 주소를 저장하는 포인터의 주소를 저장합니다.
- 함수 내에서 포인터 자체를 변경하거나, 동적 메모리 할당 결과를 호출자에게 반환할 때 사용
- **기본 사용 및 함수 내 포인터 변경:**
    
    ```c
    #include <stdio.h>
    int main() {
        int a = 10;
        int *p = &a;   // int형 변수 a의 주소를 저장하는 포인터
        int **pp = &p; // p의 주소를 저장하는 이중 포인터
        printf("a의 값: %d\n", a);
        printf("p가 가리키는 값: %d\n", *p);
        printf("pp가 가리키는 값(포인터 p의 주소): %p\n", (void*)pp);
        printf("pp가 가리키는 주소의 값(= *pp, 즉 a의 값): %d\n", **pp);
        return 0;
    }
    
    ```
    
    **함수에서 포인터 변경 예제:**
    
    ```c
    #include <stdio.h>
    void changePointer(int **pp) {
        static int b = 20;
        *pp = &b; // *pp를 변경하여 새로운 주소를 가리키도록 함
    }
    int main() {
        int a = 10;
        int *p = &a;
        printf("변경 전 p가 가리키는 값: %d\n", *p);
        changePointer(&p); // p의 주소를 전달하여 포인터를 변경
        printf("변경 후 p가 가리키는 값: %d\n", *p);
        return 0;
    }
    
    ```
    
- **동적 메모리 할당에서의 사용:**
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    void allocateMemory(int **p) {
        *p = (int*)malloc(sizeof(int));
        if (*p != NULL) {
            **p = 42;
        }
    }
    int main() {
        int *p = NULL;
        allocateMemory(&p);
        if (p != NULL) {
            printf("할당된 메모리의 값: %d\n", *p);
            free(p);
        }
        return 0;
    }
    
    ```
    
- **2차원 배열 동적 할당 예제:**
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    int main() {
        int rows = 3, cols = 4;
        int **matrix = (int**)malloc(rows * sizeof(int*));
        for (int i = 0; i < rows; i++) {
            matrix[i] = (int*)malloc(cols * sizeof(int));
            for (int j = 0; j < cols; j++) {
                matrix[i][j] = i * cols + j;
            }
        }
        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                printf("%d ", matrix[i][j]);
            }
            printf("\n");
        }
        for (int i = 0; i < rows; i++) {
            free(matrix[i]);
        }
        free(matrix);
        return 0;
    }
    
    ```
    

---

## 함수 포인터 (Function Pointer)

함수 포인터는 **함수의 주소를 저장**하는 포인터입니다.

- 함수를 매개변수로 전달하거나 콜백 함수를 구현할 때 사용합니다.
- **예제 코드 (기본 사용):**
    
    ```c
    #include <stdio.h>
    int add(int a, int b) {
        return a + b;
    }
    int main() {
        int (*funcPtr)(int, int);  // 함수 포인터 선언
        funcPtr = add;             // 함수의 주소 할당
        printf("10 + 20 = %d\n", funcPtr(10, 20));  // 함수 포인터를 통해 호출
        return 0;
    }
    
    ```
    
- **함수 포인터를 매개변수로 전달 (콜백 함수):**
    
    ```c
    #include <stdio.h>
    int subtract(int a, int b) { return a - b; }
    void calculate(int x, int y, int (*operation)(int, int)) {
        printf("결과: %d\n", operation(x, y));
    }
    int main() {
        calculate(10, 5, subtract); // subtract 함수 전달
        return 0;
    }
    
    ```
    
- **함수 포인터 배열 및 typedef 사용:**
    
    ```c
    #include <stdio.h>
    typedef int (*Operation)(int, int);
    int multiply(int a, int b) { return a * b; }
    void execute(Operation op, int x, int y) {
        printf("결과: %d\n", op(x, y));
    }
    int main() {
        execute(multiply, 5, 4);
        return 0;
    }
    
    ```
    

---

## 7. 구조체와 포인터

구조체는 여러 데이터를 하나의 타입으로 묶는 사용자 정의 자료형입니다.

- **구조체 포인터:**
    
    구조체 변수의 주소를 저장하는 포인터로, `>` 연산자를 사용하여 구조체 멤버에 접근합니다.
    
- **예제 코드 (기본 구조체 포인터 사용):**
    
    ```c
    #include <stdio.h>
    struct Person {
        char name[20];
        int age;
    };
    int main() {
        struct Person p1 = {"Alice", 25};
        struct Person *ptr = &p1;
        printf("이름: %s, 나이: %d\n", ptr->name, ptr->age);
        return 0;
    }
    
    ```
    
- **동적 메모리 할당과 구조체 포인터:**
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    struct Person {
        char name[20];
        int age;
    };
    int main() {
        struct Person *ptr = (struct Person*)malloc(sizeof(struct Person));
        if (ptr == NULL) {
            printf("메모리 할당 실패\n");
            return 1;
        }
        printf("이름 입력: ");
        scanf("%s", ptr->name);
        printf("나이 입력: ");
        scanf("%d", &ptr->age);
        printf("이름: %s, 나이: %d\n", ptr->name, ptr->age);
        free(ptr);
        return 0;
    }
    
    ```
    
