# 구조체 예제 - 정렬 구현하기
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define MAX_DEPOSITS 5  // 최대 고객 수

typedef struct {
    char name[30];
    char account_number[10];
    struct tm start_date;
    int deposit_months;
    double deposit_amount;
} FixedDeposit;

struct tm getMaturityDate(FixedDeposit* deposit) {
    struct tm maturityDate = deposit->start_date;
    maturityDate.tm_mon += deposit->deposit_months;
    maturityDate.tm_year += maturityDate.tm_mon/12;
    maturityDate.tm_mon %= 12;

    return maturityDate;
}

int compareByMaturityDate(const void* a, const void* b) {
    const FixedDeposit* fdA = (const FixedDeposit*)a;
    const FixedDeposit* fdB = (const FixedDeposit*)b;

    // 만기일 계산
    struct tm ma = getMaturityDate((FixedDeposit*)fdA);
    struct tm mb = getMaturityDate((FixedDeposit*)fdB);

    time_t tA = mktime(&ma);
    time_t tB = mktime(&mb);

    double diff = difftime(tA, tB);

    // 만기일이 더 빠른 순으로 정렬
    if (diff < 0) return -1;  // A가 더 빠름
    else if (diff > 0) return 1;  // B가 더 빠름
    else return 0; // 같음
}

void sortByMaturityDate(FixedDeposit* deposits, int n) {
    qsort(deposits, n, sizeof(FixedDeposit), compareByMaturityDate);
}



char checkMaturity(struct tm maturityDate) {
    // 현재 날짜 및 시간 얻기
    time_t t = time(NULL);
    struct tm tm = *localtime(&t);



    // return difftime(mktime(&tm), mktime(&maturityDate))/(24*60*60);
    return difftime(mktime(&tm), mktime(&maturityDate))>0?'Y':'N';
}
void displayDeposits(FixedDeposit* deposits) {
    printf("정기예금목록\n");
    for (int i = 0; i < MAX_DEPOSITS; i++) {
        struct tm maturityDate = getMaturityDate(deposits+i);

        printf("%d. %s | 계좌: %s | 예금 시작: %d-%02d-%02d | 만기: %d-%02d-%02d | 금액: %.2lf | 만기 도래: %c\n",
                    i+1,
                    deposits[i].name, deposits[i].account_number,
                    deposits[i].start_date.tm_year + 1900, deposits[i].start_date.tm_mon + 1, deposits[i].start_date.tm_mday,
                    maturityDate.tm_year + 1900, maturityDate.tm_mon + 1, maturityDate.tm_mday,
                    deposits[i].deposit_amount,
                    checkMaturity(maturityDate));
    }


}



int main() {
    FixedDeposit deposits[MAX_DEPOSITS] = {
        {"김철수", "1234567890", {0, 0, 0, 10, 0, 124}, 12, 5000000},
        {"이영희", "9876543210", {0, 0, 0, 1, 6, 123}, 24, 3000000},
        {"박민수", "5678901234", {0, 0, 0, 15, 2, 123}, 18, 2000000},
        {"조수빈", "3456789012", {0, 0, 0, 5, 8, 123}, 6, 1000000},
        {"정하나", "6789012345", {0, 0, 0, 20, 10, 123}, 12, 7000000}
    };

    sortByMaturityDate(deposits, MAX_DEPOSITS);


    displayDeposits(deposits);

    return 0;
}
```


# 1. `compareByMaturityDate()` 함수 동작 원리

```c
/**
 * @brief  만기일 비교 함수(qsort 용)
 * @param  a, b: 비교할 FixedDeposit 요소의 포인터
 * @return -1(앞이 먼저), 1(뒤가 먼저), 0(동일)
 */
int compareByMaturityDate(const void* a, const void* b) {
    const FixedDeposit* fdA = (const FixedDeposit*)a;
    const FixedDeposit* fdB = (const FixedDeposit*)b;

    /* A, B의 만기일 계산 */
    struct tm matA = getMaturityDate(fdA);
    struct tm matB = getMaturityDate(fdB);

    time_t timeA = mktime(&matA);
    time_t timeB = mktime(&matB);

    if (timeA < timeB) return -1;  // A의 만기일이 더 빠름
    if (timeA > timeB) return  1;  // B의 만기일이 더 빠름
    return 0;                      // 만기일 동일
}

```

1. **인자(`a`, `b`)**
    - `qsort()` 함수는 내부적으로 배열의 임의 요소 2개를 선택하여, 정렬 순서를 결정하기 위해 비교 함수를 호출합니다.
    - 이때 비교 함수는 반드시 다음 시그니처를 가져야 합니다:
        
        ```c
        int compare(const void* a, const void* b);
        
        ```
        
    - 즉, **`void*`(포인터 타입)로 받기 때문에**, 실제 배열 요소 타입(여기서는 `FixedDeposit`)으로 **캐스팅**해야 합니다.
2. **만기일 계산**
    - `getMaturityDate(fdA)`와 `getMaturityDate(fdB)`를 통해 각각의 만기일(`struct tm`)을 구함.
    - 이 만기일 구조체를 `mktime()`로 변환하면, 유닉스 시간(초 단위의 `time_t`)이 됩니다.
3. **timeA와 timeB 비교**
    - `timeA < timeB`인 경우, 즉 “A의 만기일이 B보다 더 이른 날짜(과거)”이면 **오름차순 정렬** 기준에서 A가 앞에 와야 하므로 `1`을 리턴.
        - **`qsort()`에서** “-1”은 “앞(a)이 뒤(b)보다 작다”를 의미함.
    - `timeA > timeB`인 경우, “A의 만기일이 B보다 늦은 날짜(미래)”이므로 A가 뒤로 가야 하므로 `1`을 리턴.
    - 같다면 `0`을 리턴해 “동등하다”라고 알림.

> 정리: 이 함수를 통해, “만기일을 기준으로 이른 날짜 → 늦은 날짜” 순으로 정렬하도록 정의해둔 것입니다.
> 

---

# 2. C 언어에서의 정렬, 비교 함수 개념

## 2.1 `qsort()` 함수

C 표준 라이브러리(`<stdlib.h>`)에서 제공되는 정렬 함수로, **퀵소트 알고리즘**을 기반으로 합니다(내부 구현은 구현체에 따라 최적화될 수 있음).

**프로토타입**:

```c
void qsort(
    void *base,      // 정렬할 배열의 시작 주소
    size_t num,      // 배열 요소(원소)의 개수
    size_t size,     // 각 요소의 크기 (바이트 단위)
    int (*compar)(const void*, const void*)
);

```

1. `base`: 정렬 대상 배열의 **시작 주소**(예: `deposits`)
2. `num`: 배열의 총 **원소 개수**(예: `n`)
3. `size`: **각 원소의 크기**(예: `sizeof(FixedDeposit)`)
4. `compar`: **비교 함수의 포인터**. 배열의 임의 두 요소를 비교해서 정렬 순서를 결정하는 역할.

**동작 순서**:

- `qsort()`는 내부에서 배열 요소 중 임의의 2개를 선택하여 `compar()` 함수에 전달.
- `compar()`가 반환한 값(-1, 0, 1 등)을 사용해 요소들의 상대적 순서를 바꿈.
- 전체 배열이 정렬될 때까지 이 과정을 반복(퀵소트 로직).

## 2.2 비교 함수(`int (*compar)(const void*, const void*)`)

**역할**: 배열 내 임의 2개 요소(a, b)에 대해, “a < b, a = b, a > b”를 판단하여 -1, 0, 1(혹은 음수/양수/0)을 반환. 이 값으로 `qsort()`가 요소들의 위치를 결정합니다.

### 일반적인 구현 패턴

```c
int compareFunction(const void* a, const void* b) {
    // 1) a, b를 실제 자료형으로 캐스팅 (예: int*, double*, 구조체*)
    // 2) 내부 필드를 비교
    // 3) a < b => 음수 리턴 (ex: -1)
    //    a = b => 0     리턴
    //    a > b => 양수 리턴 (ex: +1)
}

```

- **return 음수** ⇒ “앞(a)가 뒤(b)보다 작다” → a가 더 앞쪽
- **return 양수** ⇒ “앞(a)가 뒤(b)보다 크다” → a가 더 뒤쪽
- **return 0** ⇒ “동등” → 순서를 바꿀 필요 없음

> 실제로 C 표준에서는 “음수, 0, 양수”로 판정하기만 하면 되므로, -1, 0, 1을 꼭 사용하지 않고 (a - b) 같은 방식으로 반환하기도 합니다. 하지만 구조체 등에서 특정 필드끼리 비교할 때는 보통 if문으로 나누어 명시적으로 -1, 0, 1을 반환합니다.
> 

---

# 3. `sortByMaturityDate()`의 작동 흐름

```c
/**
 * @brief  예금 목록을 만기일 기준으로 정렬한다.
 * @param  deposits: 예금 배열
 * @param  n: 예금 개수
 */
void sortByMaturityDate(FixedDeposit* deposits, int n) {
    qsort(deposits, n, sizeof(FixedDeposit), compareByMaturityDate);
}

```

1. `qsort()` 호출 시, 배열 `deposits`의 시작 주소와 요소 개수(`n`), 요소 크기(`sizeof(FixedDeposit)`)를 넘김.
2. 내부적으로 `compareByMaturityDate()`를 여러 번 호출하며,
    - 만기일이 더 **이른** 요소와 더 **늦은** 요소의 순서를 바꾸는 과정을 반복.
3. 모든 요소가 **오름차순**(이른 날짜부터)으로 정렬된 뒤 함수 종료.

---

# 4. 추가 팁 및 응용

1. **내림차순 정렬**:
    - `compareByMaturityDate()` 내에서 `timeA < timeB`인 경우 +1을 리턴하고, `timeA > timeB`인 경우 -1을 리턴하면 만기일이 늦은 것부터 앞으로 배치됩니다.
2. **다중 키(key) 정렬**:
    - 예를 들어, “만기일이 같으면 예금액이 큰 것부터 앞에 둔다” 같은 조건이 필요하다면, 비교 로직에서 만기일이 같을 경우 예금액 필드를 비교하는 코드를 추가합니다.
3. **정렬 안정성**:
    - C의 `qsort()`는 **불안정 정렬(unstable sort)**입니다. 즉, 같다고 판단된 요소들의 기존 순서가 유지된다는 보장이 없습니다. 만약 **안정 정렬**이 필요하다면 직접 구현하거나 다른 라이브러리를 써야 합니다.
4. **범용성**:
    - `qsort()`는 `void*`와 **비교 함수**만 정의해주면 **어떤 타입**이라도 정렬할 수 있습니다(배열만 가능).
    - C++에서는 `std::sort` 등을 쓰는 것이 훨씬 편하지만, 순수 C에서는 `qsort()`가 표준적이고 범용적인 정렬 함수입니다.

---

## 정리

1. **비교 함수(`compareByMaturityDate`)**
    - `void*`로 받은 두 요소(여기서는 `FixedDeposit` 구조체 포인터)를 만기일(연·월·일 → `time_t`) 기준으로 비교.
    - 오름차순 정렬을 위해, 만기일이 이른 쪽이 “작다(-1)”, 늦은 쪽이 “크다(1)”로 반환.
2. **정렬 함수(`sortByMaturityDate`)**
    - C 표준의 `qsort()`를 호출, 배열의 시작 주소와 요소 개수, 요소 크기, 그리고 **비교 함수**를 인자로 넘겨줌.
    - `qsort()`는 내부적으로 compare 함수를 반복 호출하면서 **퀵소트 알고리즘**(또는 해당 C 라이브러리의 구현 알고리즘)에 따라 요소들을 재배치.
3. **C 언어 정렬 핵심**
    - **배열**에 대해서는 `qsort()` + **비교 함수**를 이용해 간단히 정렬 가능.
    - 비교 함수에서 “음수/0/양수”를 반환하여 순서를 결정.
