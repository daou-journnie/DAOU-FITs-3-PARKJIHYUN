### **C언어 파일 입출력 (File I/O)**

## **1️⃣ 파일 모드 (File Mode)**
파일을 열 때 사용하는 모드는 **파일을 어떻게 다룰 것인지 결정**
💡 **파일 모드 요약표**

| 모드 | 의미 | 파일이 없을 때 | 기존 데이터 |
|------|------|--------------|------------|
| `"r"`  | 읽기 모드 (Read) | 오류 발생 | 유지 |
| `"w"`  | 쓰기 모드 (Write) | 새 파일 생성 | 기존 내용 삭제 |
| `"a"`  | 추가 모드 (Append) | 새 파일 생성 | 기존 내용 유지, 새 데이터 추가 |
| `"r+"` | 읽기 & 수정 모드 | 오류 발생 | 유지 |
| `"w+"` | 읽기 & 쓰기 (수정) 모드 | 새 파일 생성 | 기존 내용 삭제 |
| `"a+"` | 읽기 & 추가 모드 | 새 파일 생성 | 기존 내용 유지, 새 데이터 추가 |

---

## **2️⃣ 주요 파일 처리 함수**
💡 **파일을 처리하는 주요 함수 정리**

| 함수 | 역할 |
|------|------|
| `fopen(filename, mode)`  | 파일을 열고 포인터 반환 |
| `fclose(fp)`  | 파일 닫기 |
| `fprintf(fp, "내용")`  | **텍스트 파일**에 포맷 지정하여 쓰기 |
| `fscanf(fp, "%s", 변수)`  | **텍스트 파일**에서 형식 지정하여 읽기 |
| `fwrite(데이터, 크기, 개수, fp)`  | **이진 파일**에 데이터 저장 |
| `fread(변수, 크기, 개수, fp)`  | **이진 파일**에서 데이터 읽기 |
| `fseek(fp, 위치, 기준점)`  | 파일의 특정 위치로 이동 |
| `ftell(fp)`  | 현재 파일 위치 반환 |
| `rewind(fp)`  | 파일의 처음으로 이동 |
| `remove(filename)`  | 파일 삭제 |
| `rename(파일1, 파일2)`  | 파일 이름 변경 |

---

## **3️⃣ 파일 쓰기 (Write)**
💡 **텍스트 파일 쓰기 예제 (`w` 모드)**
```c
#include <stdio.h>

int main() {
    FILE *fp = fopen("data.txt", "w");  // 파일 쓰기 모드 (기존 내용 삭제)
    if (!fp) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fprintf(fp, "이것은 파일 쓰기 예제입니다.\n");
    fprintf(fp, "C언어에서 파일을 다루는 방법을 배우고 있습니다.\n");

    fclose(fp);
    printf("✅ 파일 저장 완료: data.txt\n");
    return 0;
}
```
**📌 실행 후 `data.txt` 파일 내용**
```
이것은 파일 쓰기 예제입니다.
C언어에서 파일을 다루는 방법을 배우고 있습니다.
```

---

## **4️⃣ 파일 읽기 (Read)**
💡 **텍스트 파일 읽기 예제 (`r` 모드)**
```c
#include <stdio.h>

int main() {
    FILE *fp = fopen("data.txt", "r");  // 읽기 모드
    if (!fp) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char line[100];
    printf("[파일 내용]\n");
    while (fgets(line, sizeof(line), fp)) {  // 한 줄씩 읽기
        printf("%s", line);
    }

    fclose(fp);
    return 0;
}
```
**📌 실행 결과**
```
[파일 내용]
이것은 파일 쓰기 예제입니다.
C언어에서 파일을 다루는 방법을 배우고 있습니다.
```

---

## **5️⃣ 파일 수정 (기존 데이터 변경)**
💡 **파일에서 특정 데이터를 찾아서 수정하는 방법 (`r+` 모드)**
```c
#include <stdio.h>
#include <string.h>

int main() {
    FILE *fp = fopen("data.txt", "r+");  // 수정 가능 모드
    if (!fp) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char line[100];
    long pos;
    while (fgets(line, sizeof(line), fp)) {
        if (strstr(line, "파일 쓰기 예제")) {  // 특정 문자열 찾기
            pos = ftell(fp);  // 현재 파일 위치 저장
            fseek(fp, pos - strlen(line), SEEK_SET);  // 수정할 위치로 이동
            fprintf(fp, "이것은 파일 **수정** 예제입니다.\n");
            break;
        }
    }

    fclose(fp);
    printf("✅ 파일 수정 완료\n");
    return 0;
}
```
**📌 `data.txt` 파일 수정 후 결과**
```
이것은 파일 **수정** 예제입니다.
C언어에서 파일을 다루는 방법을 배우고 있습니다.
```

---

## **6️⃣ 특정 데이터 수정 (임시 파일 사용)**
기존 파일에서 **특정 데이터를 수정하려면**, **새로운 임시 파일을 만들어 변경된 데이터를 저장한 후 기존 파일을 교체하는 방식**을 사용합니다.

💡 **파일에서 특정 줄을 찾아 수정 후 저장**
```c
#include <stdio.h>
#include <stdlib.h>

#define FILE_NAME "data.txt"
#define TEMP_FILE "temp.txt"

void updateFile() {
    FILE *fp = fopen(FILE_NAME, "r");
    FILE *temp = fopen(TEMP_FILE, "w");

    if (!fp || !temp) {
        printf("파일을 열 수 없습니다.\n");
        return;
    }

    char line[256];
    while (fgets(line, sizeof(line), fp)) {
        if (strstr(line, "파일 쓰기 예제")) {
            fprintf(temp, "이것은 **변경된** 내용입니다.\n");  // ✅ 수정된 내용 저장
        } else {
            fprintf(temp, "%s", line);  // ✅ 기존 내용 유지
        }
    }

    fclose(fp);
    fclose(temp);

    remove(FILE_NAME);  // ✅ 기존 파일 삭제
    rename(TEMP_FILE, FILE_NAME);  // ✅ 임시 파일을 원본 파일로 변경
    printf("✅ 파일 수정 완료\n");
}

int main() {
    updateFile();
    return 0;
}
```

---

## **7️⃣ 이진 파일 (Binary File) 저장 & 불러오기**
💡 **이진 파일 저장 (`fwrite()`)**
```c
#include <stdio.h>

typedef struct {
    int id;
    char name[50];
    double balance;
} Customer;

int main() {
    FILE *file = fopen("customers.dat", "wb");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Customer c1 = {1001, "Alice", 50000.00};
    fwrite(&c1, sizeof(Customer), 1, file);

    fclose(file);
    printf("✅ 이진 파일 저장 완료\n");
    return 0;
}
```

💡 **이진 파일 불러오기 (`fread()`)**
```c
#include <stdio.h>

typedef struct {
    int id;
    char name[50];
    double balance;
} Customer;

int main() {
    FILE *file = fopen("customers.dat", "rb");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Customer c;
    fread(&c, sizeof(Customer), 1, file);

    printf("ID: %d | 이름: %s | 잔액: %.2lf\n", c.id, c.name, c.balance);

    fclose(file);
    return 0;
}
```
**📌 실행 결과**
```
ID: 1001 | 이름: Alice | 잔액: 50000.00
```
