
## 1. 필터(Authentication Filter)의 역할

- **목적:**  
  - 사용자가 보호된 자원(예: 대시보드, 주문 페이지 등)에 접근하기 전에 로그인 여부를 확인합니다.
  - 로그인하지 않은 사용자는 로그인 페이지로 리다이렉트합니다.
  
- **동작 방식:**  
  - 요청이 들어오면 필터가 먼저 실행되어 세션에 로그인 관련 속성(예: "memberId")이 있는지 확인합니다.
  - 특정 URL(로그인 페이지, 정적 리소스 등)은 인증 없이 접근할 수 있도록 예외 처리합니다.
  - 인증된 경우에는 요청을 계속 진행하고, 인증되지 않은 경우에는 로그인 페이지로 리다이렉트합니다.

---

## 2. AuthenticationFilter 예제 코드

**AuthenticationFilter.java**

```java
package org.example.mocktradehub.filter;

import javax.servlet.*;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import java.io.IOException;

public class AuthenticationFilter implements Filter {

    @Override
    public void init(FilterConfig filterConfig) throws ServletException {
        // 초기화 작업 (필요한 경우)
    }

    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
            throws IOException, ServletException {
        HttpServletRequest req = (HttpServletRequest) request;
        HttpServletResponse res = (HttpServletResponse) response;

        HttpSession session = req.getSession(false);
        boolean loggedIn = (session != null && session.getAttribute("memberId") != null);

        // 로그인 페이지 및 정적 리소스는 인증 예외 처리
        String loginURI = req.getContextPath() + "/login";
        boolean loginRequest = req.getRequestURI().equals(loginURI);
        boolean resourceRequest = req.getRequestURI().startsWith(req.getContextPath() + "/css/") ||
                                  req.getRequestURI().startsWith(req.getContextPath() + "/js/");

        if (loggedIn || loginRequest || resourceRequest) {
            // 인증된 사용자, 로그인 요청, 또는 정적 리소스 요청이면 필터 체인 계속 진행
            chain.doFilter(request, response);
        } else {
            // 인증되지 않은 경우 로그인 페이지로 리다이렉트
            res.sendRedirect(loginURI);
        }
    }

    @Override
    public void destroy() {
        // 자원 정리 작업 (필요한 경우)
    }
}
```

---

## 3. 웹 애플리케이션에 필터 설정 (web.xml)

만약 Servlet 3.0+의 애노테이션을 사용하지 않고 web.xml에서 필터를 등록하고 싶다면 아래와 같이 작성할 수 있습니다.

```xml
<filter>
    <filter-name>AuthenticationFilter</filter-name>
    <filter-class>org.example.mocktradehub.filter.AuthenticationFilter</filter-class>
</filter>
<filter-mapping>
    <filter-name>AuthenticationFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>
```

이 설정은 모든 요청에 대해 AuthenticationFilter를 적용합니다.  
필요에 따라 특정 URL 패턴만 필터링하도록 수정할 수 있습니다.

---

## 4. 요약

- **역할:** 로그인 여부를 확인하여 보호된 자원에 대한 접근을 제어합니다.
- **구현:**  
  - `javax.servlet.Filter` 인터페이스를 구현한 AuthenticationFilter 클래스를 작성합니다.
  - 요청에서 세션을 확인하고, 로그인 페이지나 정적 리소스는 예외 처리합니다.
  - 인증되지 않은 요청은 로그인 페이지로 리다이렉트합니다.
- **설정:**  
  - web.xml에 필터를 등록하거나, Servlet 3.0 이상의 경우 애노테이션으로도 설정할 수 있습니다.

이 자료를 참고하여 필요에 맞게 로그인 관련 필터를 구현하고 설정할 수 있습니다. 추가 질문이나 수정 사항이 있으면 언제든 말씀해주세요!
