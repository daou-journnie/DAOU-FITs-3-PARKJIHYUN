### 댓글 게시판 구현 실습


### 1. 댓글 테이블과 외래 키(FK) 사용

- **댓글 테이블에 게시글 ID(FK)를 넣은 이유**  
  - 댓글은 특정 게시글에 종속된 데이터이므로, 데이터 무결성을 유지하기 위해 게시글의 고유 식별자(예: BOARD 테이블의 id)를 FK로 참조함.
  - 이를 통해, 댓글이 어느 게시글에 속하는지 명확히 구분할 수 있고, JOIN 쿼리를 통해 게시글과 댓글을 쉽게 연결할 수 있음.

- **FK를 넣었더니 삭제 시 무결성 문제가 생긴 이유**  
  - 기본적으로 부모 테이블(BOARD)의 행을 삭제할 때, 자식 테이블(COMMENTS)에 해당하는 FK 값이 남아 있으면, 데이터베이스는 참조 무결성을 위반한다고 판단함.
  - 이 때문에 BOARD 행을 삭제하면 FK 제약 조건 위반(ORA-02292, ORA-02291 등) 오류가 발생함.

- **해결 방법: Cascade Delete**  
  - FOREIGN KEY 제약 조건에 `ON DELETE CASCADE` 옵션을 추가하면, 부모 테이블에서 행이 삭제될 때 해당하는 자식 테이블의 행들도 자동으로 삭제됨.
  - 예시 쿼리:  
    ```sql
    CONSTRAINT fk_board FOREIGN KEY (boardId) REFERENCES BOARD(id) ON DELETE CASCADE
    ```
  - 이를 통해, 게시글이 삭제되면 관련 댓글들도 깨끗하게 함께 삭제되어 무결성 문제가 해결됨.

---

### 2. 테이블 생성과 VO(DB Table) 설계 고민

- **VO와 DB 테이블은 일치해야 하는가?**  
  - 꼭 1:1 대응될 필요는 없음.  
  - VO는 애플리케이션에서 데이터를 다루기 위한 객체로, DB 테이블의 스키마와 다르게 추가적인 필드(예: 화면에 표시하기 위한 계산된 값, 조인 결과 등)를 포함할 수 있음.
  - 예를 들어, BoardVO에 `writer` 필드가 포함되어 있는데, 이는 BOARD 테이블에는 없고 MEMBER 테이블과의 조인 결과로 채워질 수 있음.

- **게시글(BOARD) 테이블에 어떤 컬럼을 넣어야 하는가?**  
  - 기본적으로 필요한 컬럼: id, title, content, user_id, like_count, comment_count, regDate, modDate, view_count 등.
  - VO에서는 추가적으로 화면 표시를 위한 값(예: 작성자 이름(writer))이나, 로직상 필요한 필드를 가질 수 있음.
  - DB에서는 데이터 무결성과 효율적인 쿼리를 고려해 최소한의 핵심 데이터만 저장하고, 필요한 경우 JOIN이나 별도의 조회로 추가 정보를 얻는 방식이 좋음.

- **JOIN을 이용한 쿼리 사용**  
  - 게시글 작성자의 이름, 댓글 수, 좋아요 수 등은 여러 테이블에 분산되어 있을 수 있음.
  - SQL JOIN을 활용하여 여러 테이블의 데이터를 결합, VO에 필요한 정보를 채워 넣을 수 있음.
  - 예를 들어, 댓글 출력 시 COMMENTS와 MEMBER 테이블을 조인하여, COMMENTVO의 `writer` 필드에 회원 이름을 매핑하는 방식.
  - 이런 방법은 DB 레벨에서 필요한 데이터를 한 번에 가져올 수 있어, 애플리케이션 로직에서 별도의 후처리 없이 바로 사용할 수 있음.


